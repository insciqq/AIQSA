Use case: ui-mockup
Asset type: shippable desktop AIQSA group access administration mockup
Input image: Image 1 is a visual-style reference only. Preserve its light paper/mineral palette, charcoal typography, oxidized-teal interaction accent, flat data hierarchy, quiet separators, restrained radii, and precise enterprise polish. Rebuild all navigation and content to the specification.

Create a front-on 2048x1152 AIQSA Admin console with the same real grouped navigation as the Team Users mockup, but select "Model access". No browser chrome and no invented destinations.

Main header exactly "Model access" with sentence "Toggle provider, model, and search access for active groups.". Left subpanel is a searchable group list with "Research" selected and "Operators" below; show Active status and member counts.

Main detail is for group "Research" and contains only real capability sections:
1. "Provider-wide access" with OpenAI enabled, Anthropic disabled, OpenRouter disabled.
2. "Explicit model grants" grouped by provider. Show GPT-5.6 Terra granted and two other models not granted. Include quiet actions exactly "Grant all models" and "Clear models".
3. "Search strategy grants" with one enabled strategy and one disabled strategy.
4. "MCP server grants" with one disabled server row.

Show a restrained informational notice exactly "Members granted this model will use the provider's Default credential.". Do not show or edit provider keys here. Do not invent direct user overrides, access bundles, billing, audit, or a global Save button if toggles settle individually.

Constraints: locally scrollable matrices where needed, readable labels, no gradients, glassmorphism, card wall, horizontal global tabs, excessive pills, competitor branding, or watermark.
