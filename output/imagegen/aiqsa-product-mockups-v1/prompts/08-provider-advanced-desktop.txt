Use case: ui-mockup
Asset type: shippable desktop AIQSA advanced provider administration mockup
Input image: Image 1 is a visual-style reference only. Preserve its light paper/mineral palette, charcoal typography, oxidized-teal action accent, flat list/detail structure, subtle separators, restrained radii, and serious precise operational polish. Rebuild the screen content to match the real AIQSA provider capabilities below.

Create a front-on 2048x1152 AIQSA Admin console. Use the same real grouped global navigation as the Personal setup mockup, with "Providers" selected and no invented destinations. No browser chrome.

Main header exactly "Providers". A narrow connection list contains OpenAI selected with states "Enabled" and "Active v3", Anthropic "Not configured", and OpenRouter "Changes pending". Above it: "Connections", "Refresh providers", "New connection".

Selected OpenAI detail header: "OpenAI", "Enabled", "Active v3", and quiet action "Activate changes" only if needed. Use a local vertical section index made only from real existing sections: "Run profiles", "Connection", "Credentials", "Key assignment", "Models", "Diagnostics and troubleshooting". Select "Credentials".

Credentials detail:
- heading "Credentials" and helper "Keys are write-only. Saved values are never returned.";
- existing row "Primary" with status "Active key v2" and actions "Rename", "Rotate", "Disable credential";
- collapsed "Add key" form;
- separate quiet danger area containing "Revoke active key" and "Delete credential";
- a short context line "Users without a group key · Use default credential" with link "Review key assignment".

Show no secret value. Do not add connection/model revision history, rollback, provider Activity, audit log, Test connection, model sync metrics, fallback model, retries, logging, arbitrary headers, or provider-level temperature. Optional paid checks belong only under the collapsed real "Diagnostics and troubleshooting" section.

Constraints: expert controls remain navigable but do not form an endless stacked page; no gradients, glassmorphism, KPI dashboard, horizontal global tabs, card mosaic, fake metrics, competitor branding, or watermark.
