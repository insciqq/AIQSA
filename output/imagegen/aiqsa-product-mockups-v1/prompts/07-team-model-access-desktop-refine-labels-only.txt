Use case: ui-mockup
Asset type: surgical label correction in an existing desktop AIQSA Model access mockup

Edit only the two small transparent masked row regions. Preserve every unmasked pixel exactly, including the real global sidebar, group list, Provider-wide access section, information notice, all headings, provider group rows, MCP server grants, borders, toggles, typography, spacing, and palette.

In the masked OpenAI model rows, render exactly three rows in the existing structure:
- "GPT-5.6 Terra" enabled;
- "GPT-5.6 Luna" disabled;
- "GPT-5.6 Sol" disabled.

In the masked Search strategy grants rows, render exactly:
- "OpenAI web_search" enabled;
- "Perplexity tool" disabled.

Do not show Knowledge search, generic Web search, GPT-5, GPT-4o, knowledge bases, connected sources, direct user overrides, billing, audit, or new controls. Do not remove or move Provider-wide access, the notice, collapsed Anthropic/OpenRouter rows, or MCP server grants.

Constraints: exact legible English text; preserve the existing row geometry and toggles; change only masked pixels; no solid-color artifact, new content, gradient, watermark, or browser chrome.
