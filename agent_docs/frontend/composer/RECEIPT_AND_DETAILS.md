# LEGACY FRONTEND DOCUMENT PATH

The active contract moved to [Answer outputs and Branches](ANSWER_OUTPUTS_AND_BRANCHES.md).

This compatibility file exists only because the current documentation manifest still requires the historical path. The implementation task that removes the old UI must update `scripts/docs-manifest.mjs` and delete this file. It defines no product behavior.
