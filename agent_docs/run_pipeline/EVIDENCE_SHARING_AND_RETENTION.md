# LEGACY RUN-PIPELINE DOCUMENT PATH

The active contract moved to [Outputs, recovery, sharing, and retention](OUTPUTS_RECOVERY_SHARING_AND_RETENTION.md).

This compatibility file exists only because the current documentation manifest still requires the historical path. The implementation task that removes the old inspection subsystem must update `scripts/docs-manifest.mjs` and delete this file. It defines no run behavior.
