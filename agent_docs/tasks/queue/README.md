# TASK QUEUE

This directory is the executable unfinished-task ledger. The [task queue manual](../README.md#queue-and-selection) owns selection, lifecycle, dependencies, commands, and task shape; queue task instances are ignored, and only this README is tracked.
