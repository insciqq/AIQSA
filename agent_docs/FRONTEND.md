# FRONTEND

## Current Product Contract

ADRs 0025, 0028, and 0030 define the shipped Research Chat, direct composer controls, compact completed-answer evidence, and task-first Control Center presentation. They retain ADR 0009's complete capability requirement and ADR 0011's single control ownership while amending earlier presentation, theme-default, compact-composition, and plan-like admin-navigation decisions. ADRs 0026, 0028, and 0030 own the four-provider reviewed-model Quick setup path and its direct-user credential assignment without replacing the advanced provider control plane; ADR 0029 owns the built-in `Full access` group and its automatic current/future resource coverage. ADR 0021 still owns admin-managed MCP plus persistent user enablement/configuration, ADRs 0022-0023 own administrator-managed LLM providers and runtime email delivery, and ADR 0024 owns the three semantic run-profile mappings. Presentation may change only while server contracts, entitlements, run semantics, store/action ownership, and the complete capability inventory below remain reachable.

## Presentation Boundary

The clean-slate presentation is the only runtime view layer on the existing Next.js routes. Backend APIs, client contracts, domain modules, actions, and the focused workspace/thread/composer/run stores remain the only state owners unless a separately accepted decision changes them. There is no `/v2`, parallel API client, classic/new preference, hidden fallback renderer, or duplicate product state.

The source map and behavioral detail below describe the currently shipped implementation. Future UI changes consume the existing feature contracts and update this document in the same change; mockups or isolated components never establish current behavior. `DESIGN_SYSTEM.md` is binding for visual work, while this file remains binding for behavior, state ownership, and responsive access.

## Deferred Accessibility Scope

Dedicated accessibility implementation and conformance are not part of the current product scope. They require a separately approved future task.

## Primary UI

The first screen is the usable Research Chat workspace; there is no landing-page product. ADRs 0025, 0028, and 0030 own its clean-slate composition, direct controls, adaptive disclosure, and centered blank-chat start state; ADR 0011 continues to own the single composer-control owner as amended by ADR 0030. ADRs 0026, 0028, and 0030 own Provider Quick setup. This file takes precedence over visual inspiration for behavior, state, and responsive UX; `DESIGN_SYSTEM.md` owns appearance.

## Capability Inventory

This is the current capability inventory. A UI change is incomplete if it removes an applicable line or hides it without a discoverable product path.

- **Workspace:** create and switch chats; blank first-send creation; search titles/provider/model and server-side message content; favorites; nested folders/projects; collapse, create, rename, move, delete, and project memory/settings; per-chat active-run cues.
- **Catalog and next-run selection:** backend-entitled concrete-model selection grouped by provider, administrator-mapped Fast/Balanced/Deep model-and-reasoning profiles, explicit search strategy, prompt preset selection, prompt management, and per-model draft restoration/clamping.
- **Run controls:** reasoning effort, background mode, provider Stream mode, citation visibility, reasoning/thinking visibility, default-on inline tool-activity visibility, answer sound, temperature, and max output tokens, with existing autosave/flush and next-run-only semantics where applicable. The three visibility controls are presentation preferences and never change an accepted provider request or tool execution.
- **Composer and attachments:** per-chat drafts, Enter/Shift+Enter, IME safety, multiline growth, edit-branch mode, file picker and drag/drop, multiple PDF/image/text-like attachments, attachment-only send, upload progress/partial failure, Send, Stop, and cross-chat concurrent runs.
- **Context and usage:** current-context estimate and warning, truncation artifact, active-branch message/token/cached-input statistics, and no user-facing estimated dollar cost.
- **Run and answer states:** catalog/workspace/chat-detail loading and error, queued/streaming/complete/cancelled/error assistant states, fake/provider streaming, QSA/search/tool activity, jump to latest, and background-run recovery/check action.
- **Answer artifacts:** safe markdown, fenced-code copy/highlighting, tables/lists/quotes/links, search summaries, compact expandable tool activity, citations, reasoning/thinking, provider artifacts/warnings, usage, and long-content containment.
- **Message and branch actions:** regenerate, edit, copy, delete subtree, Branch from here, branch checkout/active leaf, Copy thread, confirmations, and mid-stream mutation gates.
- **Details and inspection:** closed-by-default contextual Branch and Events access; readable run/status/error display; chronological grouped event meaning and counts; and true-fork/active-leaf checkout. Composer Run setup remains the only next-run parameter editor, while immutable model-run request/response inspection remains available through APIs.
- **Sharing:** Share anonymously, a visible/copyable secret link, immediate revocation for the just-created link, immutable sanitized public snapshots, and no private attachments/provider payloads/internal ids/user-group data. There is no durable share-history UI.
- **Command palette and overlays:** command palette across actions/chats/models/search/prompts, shortcut safety in text entry, picker/menu/tab interaction, close/cancel/backdrop behavior, and single-modal ownership.
- **Settings and appearance:** prompt create/edit/duplicate/delete/default/use-next-run flows, entitled MCP server enablement/readiness/personal fields/OAuth identity, dirty-close protection, six local palettes (AIQSA, Graphite, Verdant, Classic Dark, Classic Light, Paper), scheme-aware cookie-backed first paint, and project settings.
- **Auth and public access:** email/password sign in, configured-only Google/Yandex OAuth sign in with same-email account merge, verified allowlisted access requests, direct one-time invite onboarding, verification, reset request/completion, readable generic failures, recovery-token UI absence, route guards, and public-share invalid/revoked states.
- **Responsive access:** workspace and Details drawers, complete core workflow at 384-390x844, short-landscape operation at 844x390, deliberate tablet composition at 768x1024, touch-visible actions, safe overlays, no required hover, and no page-level horizontal overflow.
- **Admin:** server-guarded entry and denial; Providers, Users, Access & groups, Invites, Access rules, Usage, MCP servers, Email delivery, and Safety; full-width indexes plus dedicated resource details; approvals, direct group-membership editing, ordinary exact grants and the built-in `Full access` group's automatic current/future provider/model/search/MCP coverage, rules, invites, and session controls; direct-user and group provider credential selection; provider connection/model/credential/preflight/diagnostic/activation lifecycle plus atomic run-profile mappings; MCP draft/test/activation/update/rollback/rebuild/OAuth-validation lifecycle; SMTP save/test/activation lifecycle; deletion plus acting/final-admin guards. Legacy `groups` and `model-access` section values normalize to `access`.

## Layout

At `>=1024px`, Research Chat uses a persistent Workspace rail beside one conversation column. The conversation column owns its local top rail, thread, and composer; no global application bar spans the Workspace rail. At `>=1440px`, explicitly pinned Details adds a third normal-flow column without changing the answer's reading measure. Overlay Details never changes the grid. Exact geometry belongs to `DESIGN_SYSTEM.md`.

```text
desktop: Workspace | conversation { local top rail, thread, composer }
wide pinned: Workspace | conversation | Details
compact: conversation + on-demand Workspace/Details overlays
```

Below the 1024px desktop breakpoint, the top rail exposes adjacent Workspace and `Start new chat` actions. Workspace opens the existing left chat/folder pane as a modal drawer; direct New chat activates the existing top-level blank session without opening the drawer or persisting a row before first send. It is disabled only before workspace readiness or during first-chat creation and remains available while another saved chat runs. The drawer reuses the desktop pane actions for chat search, new chat, folder creation, chat rename/move/share/delete/export, folder rename/move/delete, subfolders, and project settings. It has an explicit touch-sized Close action, honors all safe-area edges, preserves the browse-region scroll position across close/reopen, and clears open row menus when it closes. Crossing the desktop breakpoint while it is open closes the drawer state before the CSS-hidden desktop composition can leave the shell blocked. Opening Project Settings from the drawer closes Workspace first, so only one modal layer is active.

The current-chat or `New chat` heading remains visible and truncated at every width. Compact composition keeps Workspace, direct New chat, Account, and truthful live Pipeline status in the local top rail. Share, Details, and the secondary `Conversation actions` menu appear only after the conversation has at least one message; an empty start state does not advertise actions with no object. Once available, Copy thread and Branch tree share that one secondary menu at every width, while Command palette and Settings live in Account. There is no second thread toolbar or duplicated desktop action set. Below `sm`, the leading Workspace/New chat pair drops its visual gap and the decorative product identity stays absent so direct actions remain contained at the target portrait width.

The responsive contract is verified at 384x844 portrait, 844x390 short landscape, and 768x1024 tablet in addition to the desktop evidence widths. Width chooses shell composition while the composer keeps one stable information model at every size: Message, direct Model/configured-Profile/Search controls, More, a labeled Tools entry, one Usage disclosure, Attach, and Send/addressable Stop. In the centered blank state, the Message label and separate Usage line are visually suppressed and Attach may use its icon-only compact form; all existing control owners remain. The footer wraps when space is constrained instead of switching to a second control hierarchy. Input capability chooses density; `(hover: none)` or `(pointer: coarse)` keeps required actions at least 44px even at landscape/tablet widths. The 768px layout keeps the conversation primary with drawer navigation and bounded overlays rather than exposing a cropped desktop grid.

Model and Search open their existing pickers directly, and each configured Fast/Balanced/Deep profile applies its exact model/reasoning tuple in one click. More opens the same safe-area-aware, locally scrolling complete Run setup at every viewport. The context/usage action exposes approximate input, safe input budget, total context, and provider-reported usage without displacing writing or Send/Stop. There is no scroll-triggered composer state or separate narrow/short-height control owner.

`app/layout.tsx` opts into both `viewport-fit=cover` and `interactive-widget=resizes-content`. The shell uses `h-dvh`, fixed/dialog surfaces subtract `env(safe-area-inset-*)` on every relevant edge, and each tall surface keeps one local scroll owner. Surfaces that would not fit a viewport at or below 32rem high—Run settings, model/prompt/finite pickers, Settings, and the command palette—switch to viewport-bounded sheet geometry. This contract follows content-viewport reduction from the software keyboard instead of assuming a stable `100vh` or hiding controls behind the keyboard.

Details presentation modes:

- `closed`: the fresh/default state; no Details width is reserved;
- `overlay`: a shared responsive modal drawer/sheet with backdrop, blocked background, explicit Close, and Escape/backdrop close;
- `pinned`: an optional normal-flow panel offered only at `>=1440px`.

Opening Branch, Events, or the global Details action selects the requested tab and opens `overlay` unless Details is already pinned. Run and fork changes never auto-open Details. A pinned window narrowed below 1440px demotes the currently open panel to `overlay`. Only an explicit pinned choice persists in `aiqsa.detailsMode`; the transient overlay persists as closed, and first render remains hydration-stable.

Main areas:

- Workspace rail/drawer with searchable chat and folder navigation;
- conversation column with local top rail, thread, and composer;
- on-demand Details overlay or wide-screen pinned inspection plane;
- Settings and command overlays;
- auth and public-share workspaces;
- grouped Control Center shell and its current destination.

## Implemented Components

- `app/page.tsx`: authenticated app entry.
- `app/login/page.tsx`: server entry for the mode-driven auth workspace; it accepts invite, verification, reset, privacy-safe OAuth outcome, and safe internal post-login destination parameters, passes only configured Google/Yandex provider ids to the browser, and never exposes credentials or bootstrap recovery in normal UI.
- `app/admin/page.tsx`: server-guarded admin entry that redirects anonymous/inactive sessions, shows non-admin denial without fetching admin data, and mounts the admin panel for active admins.
- `app/s/[shareToken]/page.tsx` and `not-found.tsx`: anonymous server-rendered share lookup by hashed secret token, sanitized snapshot presentation, and the single generic unavailable path.
- `components/app-shell/PowerAppShell.tsx`: shell composition root for stores, stream/session ports, focused view/action hooks, and the local UI controllers below; it no longer owns workspace menus/drafts, Details/theme, palette/drawer, or confirmation resolver state directly.
- `components/app-shell/PowerAppShellView.tsx`, `powerAppShellViewContracts.ts`, `TopRail.tsx`, `MainThreadPane.tsx`, and `ShellLeftPane.tsx`: the Research Chat shell, its exact seven-feature contract, conversation-local top rail, thread/composer pane, and Workspace wiring adapters kept separate from controller logic. At desktop widths the Workspace rail is persistent; compact widths reuse it as a drawer; optional pinned Details becomes a third normal-flow column only at `>=1440px`. `ShellLeftPane` routes chat activation through a stable event callback so the memoized left-pane view always calls the latest catalog-aware activation handler.
- `lib/contracts/admin.ts`, `adminProviders.ts`, `runProfiles.ts`, `catalog.ts`, `chats.ts`, `email.ts`, `mcp.ts`, `runs.ts`, `settings.ts`, `http.ts`, and `components/app-shell/types.ts`: client-safe admin, provider control-plane, run-profile, catalog, runtime email, MCP, summary/detail/thread, persisted-run/event, current-user settings, and shared HTTP error contracts plus runtime response decoders and frontend re-exports alongside shell-local notice and Details contracts.
- `components/app-shell/theme.ts`: local palette registry in the stable order AIQSA (`aiqsa`, dark), Graphite (`graphite`, dark), Verdant (`verdant`, dark), Classic Dark (`classic-dark`, dark), Classic Light (`neutral`, light), and Paper (`paper`, light). `paper` is appended without reordering or changing the five original ids. `neutral` remains the first-use and fully invalid-value fallback; a malformed LocalStorage value yields to a valid server-rendered cookie theme, while a valid local choice repairs a stale cookie. Runtime and first-paint application always set `data-theme` and `data-color-scheme` together.
- `components/app-shell/shellApi.ts`, `threadContent.ts`, `workspaceGroups.ts`, `controlDefaults.ts`, `shellFormatting.ts`, `shellStorage.ts`, and `shellValues.ts`: narrow pure owners for API/SSE shaping, persisted content and artifact summaries, workspace grouping, catalog control defaults, readable errors/labels, browser preferences, and shared value guards. Consumers import these owners directly; there is no broad shell utility barrel.
- `components/app-shell/usePinnedScroll.ts`: rAF-throttled thread scroll ownership, stable user-turn anchoring with bounded prior-answer context for a new streamed turn, explicit jump-to-latest recovery, and bottom-scroll reset behavior.
- `components/app-shell/useDialogFocus.ts`: session-scoped dialog focus entry/restoration with configurable autofocus, Tab containment, and Escape handling; keyboard events are owned by their innermost dialog, and a non-dialog inline editor may keep Escape local by preventing its default before the parent hook runs. Nested confirmations contain Tab and consume Escape without closing/trapping the parent, while drawer-to-pinned transitions release modal behavior without restoring focus until the whole Details session closes.
- `components/app-shell/useRunStream.ts`, `useRunStreaming.ts`, `runState.ts`, `runLifecycleStore.ts`, `runSurfaceStore.ts`, `workspaceStore.ts`, `threadStore.ts`, `composerSessionStore.ts`, `composerControlStore.ts`, and `promptSettingsStore.ts`: shared foreground SSE consumption, tolerant parse warnings, 50ms text buffering, bounded adjacent-token event aggregation, run id/message id reconciliation, keyed per-chat run inspection, thread, and composer-session caches, summary-only workspace state, and focused Zustand state slices.
- `components/app-shell/messageRunActions.ts`, `messageRunLifecycle.ts`, and `messageEditAction.ts`: composer submit/send/regenerate/edit branch actions. Send and regenerate keep their distinct optimistic/request preparation in the action owner and delegate stream start, HTTP/SSE execution, persisted-id adoption, source-chat reconciliation, terminal notification, failure/cancellation, and controller-safe cleanup to one lifecycle executor. Rejected requests roll back only their owned optimistic rows; ambiguous accepted/network failures force a source-keyed durable refresh, and a later retry first reconciles any still-stranded temporary leaf. Upload and edit awaits retain an explicit source session token; edit admits one PATCH per source session and uses detail refresh, with the decoded committed response as the network-failure fallback.
- `components/app-shell/workspaceActions.ts`, `folderActions.ts`, `promptSettingsActions.ts`, `runControlsActions.ts`, `settingsMutationCoordinator.ts`, `runLifecycleActions.ts`, and `threadActions.ts`: focused mutation/action modules for workspace, folders, prompts, serialized current-user defaults, run resume/cancel/upload, and branch/share/delete/copy behavior.
- `components/app-shell/useShellAppearanceController.ts`, `useWorkspaceInteractionController.ts`, and `useShellOverlayController.ts`: focused local owners for Details/theme persistence, workspace menu/edit/project transitions, and palette/mobile-drawer/confirmation/focus/shortcut lifecycles. They expose semantic feature ports rather than one catch-all reducer, context, global store, or root setter bag.
- `components/app-shell/usePowerAppShellViewModel.ts`, `useCommandPaletteActions.ts`, `useShellUiActions.ts`, `useAnswerNotification.ts`, `useChatContentSearch.ts`, `threadPath.ts`, and `powerAppShellData.ts`: derived shell view state, command dispatch, UI callbacks, local answer alerts, chat-content search, branch-path helpers, and shell-only control/default parsers. Catalog, workspace, chat-detail, and model-run response decoding is owned by `lib/contracts/`.
- `components/app-shell/TopRail.tsx`, `ShellNotice.tsx`, `LeftChatPane.tsx`, `ThreadMessageRow.tsx`, `ThreadArtifacts.tsx`, `RunReceipt.tsx`, `ProjectSettingsDialog.tsx`, `SettingsDialog.tsx`, and `InspectorPanels.tsx`: focused shell view modules extracted from `PowerAppShell` so pane/control/message/Settings/Details changes stay localized. `ThreadArtifacts` owns the shared collapsed/expanded search, tool-call, citation, reasoning, and context-truncation presentation; it consumes only already bounded client-safe summaries. `runReceipt.ts` derives terminal answer facts without fetching or reading current composer defaults, while `RunReceipt.tsx` renders only segments with a truthful destination as controls.
- `components/app-shell/sessionActions.ts`: same-origin authenticated logout request, stable failure mapping, and post-revocation `/login` navigation.
- `components/app-shell/ComposerControls.tsx`, `ComposerRunProfiles.tsx`, `ComposerModelPicker.tsx`, `ComposerPromptPicker.tsx`, `ComposerOptionPicker.tsx`, `composerPicker.tsx`, and `runProfiles.ts`: direct Model/configured-Profile/Search controls plus More over one complete Run setup and state owner at every viewport, server-projected opaque profile resolution, distinct picker content owners, and one local picker session primitive. Fast/Balanced/Deep remain available both directly and inside Run setup; inline and nested Model picker sessions stay isolated and do not cross the root shell/view contracts.
- `components/app-shell/McpSettingsSection.tsx`, `McpComposerSummary.tsx`, `mcpSettingsApi.ts`, and `mcpSettingsStore.ts`: entitled current-user MCP catalog/readiness, persistent multi-enable state, write-only personal fields, per-user OAuth actions/outcomes, runtime tool inventory, visible-tab refresh, and the compact read-only composer aggregate.
- `components/app-shell/eventLog.ts`: readable Details event digest.
- `components/auth/AuthLogin.tsx`: the responsive auth workspace for email/password and configured Google/Yandex sign-in, privacy-safe OAuth callback outcomes, verified access requests, direct invite acceptance, verification, generic request/check-email outcomes, and password-reset request/completion states.
- `components/share/PublicShareView.tsx`: focused read-only conversation presentation and the generic unavailable state, both independent of private shell/session assumptions.
- `components/admin/AdminPanel.tsx`, `AdminConsoleHeader.tsx`, `AdminSectionFrame.tsx`, and `AdminSectionTabs.tsx`: the Control Center composition boundary and grouped shell. `AdminPanel` mounts the shared dashboard and section controllers for their required lifetimes, wires their contracts to the active destination, and keeps the confirmation host outside the current workspace; it does not own section drafts, transport, or filtering. There is no global KPI/attention overview; attention stays with its owning destination.
- `components/admin/useAdminDashboardResource.ts`, `useAdminActionRunner.ts`, `useAdminFeedback.ts`, `useAdminFieldErrors.ts`, `useAdminConfirmationController.ts`, `useAdminOneTimeInviteLink.ts`, `useAdminSectionNavigation.ts`, and `useAdminOperationalFocus.ts`: focused admin resource/control lifetimes. Successful mutations reconcile through the canonical dashboard before clearing busy state; stale overlapping refreshes cannot replace newer data and still deliver the pending mutation destination, failed refresh preserves the last good dashboard and refresh clock, confirmations close before dispatch, and the ephemeral invite URL remains independent of dashboard refreshes and section switches.
- `components/admin/useAdminUsersController.ts`, `useAdminGroupsController.ts`, `useAdminProvidersController.ts`, `useAdminEmailController.ts`, `useAdminInvitesController.ts`, and `useAdminAccessRulesController.ts`: focused domain controllers behind narrow section props. Users owns pagination, explicit selection, and keyed membership drafts; one Groups controller owns the canonical Access & groups index and its Overview, Members, Models & search, and Tools detail tasks. The full provider controller exists only while Advanced is open and unmounts with every credential/editor draft on return to Quick; Email owns its independent lazy resource; Invites and Access rules own their persistent forms/filters and exact confirmation requests. Group-assignment drafts are projected and submitted against currently active groups after refresh, so newly archived group ids cannot leak through pending approval, invite, rule, membership, or provider-key assignment actions.
- `components/admin/AdminUsersSection.tsx`, `AdminAccessGroupsSection.tsx`, `AdminProvidersSection.tsx`, `AdminRunProfilesPanel.tsx`, `AdminMcpServersSection.tsx`, `AdminMcpDraftEditor.tsx`, `AdminMcpGrantPanels.tsx`, `AdminEmailSection.tsx`, `AdminInvitesSection.tsx`, `AdminAccessRulesSection.tsx`, `AdminUsageSection.tsx`, and `AdminSafetySection.tsx`: focused operational presentation modules behind section-specific data, draft/view, status, ref, and semantic-action contracts. Users and Access & groups use full-width indexes, whole-row selection, no automatic first selection, and dedicated full-width details. Provider, run-profile, and SMTP configuration have independent controllers; MCP server lifecycle has its own catalog/controller; ordinary group model/search/server grants stay in the selected Access & groups detail and direct-user/personal-slot MCP grants stay in Users. The `Full access` detail keeps Members editable but renders lifecycle and resource coverage as built-in read-only facts rather than impossible toggles. The modules do not own server authorization or expose stored secret values.
- `components/admin/adminUserView.ts`, `adminGroupView.ts`, `adminInviteView.ts`, `adminAccessRuleView.ts`, `adminDraftGroups.ts`, `adminSections.ts`, `adminPrimitives.tsx`, and `adminViewUtils.ts`: pure section derivation, subject-owned URL/tab mapping, active-group pruning, shared admin primitives, and display helpers. `AdminTaskWorkspace` remains available only for specialized sections whose existing workflow needs it; Users, Access & groups, and Advanced provider connections own explicit full-width index/detail composition instead of a universal desktop split. `adminApi.ts`, `adminProvidersApi.ts`, `adminRunProfilesApi.ts`, and `adminEmailApi.ts` own same-origin requests, decoding, and stable error mapping; wire schemas remain in `lib/contracts/` rather than frontend copies.
- `components/chat/Composer.tsx`: the coherent `max-w-reading` composer frame containing edit/disabled state, attachment chips, one stable Message plane, direct Model/Profile/Search plus More, labeled Tools and Usage entries, Attach, and Send/addressable Stop while preserving Enter/Shift+Enter and IME behavior. It has no scroll-triggered collapsed presentation.
- `components/chat/MarkdownMessage.tsx`: safe answer markdown for headings, tables, nested lists, blockquotes, emphasis, links, inline code, horizontal rules, and fenced code blocks.
- `components/command-palette/CommandPalette.tsx` and `commandItems.ts`: Ctrl/Cmd+K command search, current markers, filtering, Arrow navigation, Escape close, and Enter execution.
- `components/inspector/InspectorTabs.tsx`: Branch/Events tab navigation.

## Presentation And Runtime State

Palette values, typography, geometry, component appearance, responsive density, and visual hard rules live only in `agent_docs/DESIGN_SYSTEM.md`. The behavior/state contracts that styling must preserve are listed below:

- Run activity presents only already-consumed evidence. The application bar and active assistant tail use one shared status: `Working…` while the exact provider stage is unknown, `Searching…` only after a search/citation event, `Answering…` only after answer tokens, and a readable run-error state on failure. They never render speculative waiting stages. Idle/settled decoration is absent; the application-bar action opens Details Events and never auto-opens Details. Selected-off versus backend-skipped Search remains inspectable through run data and completed artifacts rather than a fabricated live sequence.
- The application bar keeps Workspace first, direct compact New chat beside it, the sole current-chat/New chat semantic heading, Share, state-aware Details, and Account. Account is always present, identifies the current canonical account email as noninteractive wrapped text, and owns Command palette, Settings, Sign out, and entitled Admin. `Conversation actions` owns Copy thread and Branch tree at every width. Model stays in composer/answer metadata. Below `lg`, the heading is visually hidden; desktop keeps the visible heading without introducing a second toolbar or duplicate action owner.
- Active-chat detail loading renders a status skeleton rather than the blank-chat state; deterministic test mode freezes its shimmer.
- Initial catalog/workspace bootstrap has explicit pending/error/ready ownership with one actionable Retry surface and disabled dependent mutations. Later refresh failure preserves usable hydrated data. Catalog/workspace retries deduplicate.
- A persisted chat with no default provider model is a valid workspace row. The wire contract uses paired nullable model/provider values, while the shell normalizes absence into its existing empty local selection and may render an available catalog fallback for the composer without silently persisting that fallback. Legacy paired empty-string responses remain readable during the compatibility window; half-populated pairs fail closed.
- Active-chat detail failure has one in-thread Retry owner and keeps the composer disabled; it never produces a duplicate shell notice or masquerades as a new chat.
- The blank-chat and zero-model states appear only after bootstrap readiness and truthfully distinguish an available empty workspace from missing administrator-granted access. The available blank state uses the concise `Ask anything.` invitation without turning the thread into marketing copy.
- Thread activation lands on the active leaf. A valid explicit Send in the still-active source chat reclaims scroll ownership after its optimistic user/assistant rows appear and brings that newest turn into view, even when the user had been reading older messages; empty/blocked sends and navigation-owned source mismatch do not move another chat. While the assistant is pending or streaming, the turn is anchored on the newest question with a viewport-bounded tail of the preceding answer left above it; the pending state remains below instead of becoming the scroll target. Temporary reading space below the live answer shrinks as it grows, and token growth preserves that turn-start anchor instead of pulling the viewport to the tail. A thread already unpinned by the user keeps its position for passive updates. The dedicated touch-safe Latest row appears only when more than 48px of the last real message content extends below the thread viewport; receipt/action-only space, temporary reading space, and terminal answer spacers never create it by themselves. Activating Latest or deliberately returning to the bottom hides the row and resumes tail following.
- Queued, live, cancelled, failed, complete-without-text, and normally complete assistant tails remain visually distinct.
- Token-only updates repaint the live tail, not memoized historical rows, Markdown/artifact blocks, or the sidebar projection.
- The composer shows ADR 0017 approximate current input against the safe input budget after output reserve and margin, names the full model context separately, and shows active-branch provider-reported usage statistics; it never renders estimated dollar costs.
- Answer completion may use the local oscillator/favicon alert, with the hidden-tab favicon signal ending when the user returns.
- General-shell and Settings notices have independent owners. Persistent workflow notices remain in flow; transient notices stay bounded; both allow manual dismissal and ordinary success auto-clears. Settings never transplants a general notice and closing it clears only its own channel. General transient notices may stay readable but noninteractive above non-Settings backdrops; Account is the higher active layer.
- Sign-out timeout/failure has a visible error cue on the closed Account trigger and full detail plus Retry when the menu reopens.
- Readable errors retain stable backend codes when present. Malformed stream frames are skipped without aborting later frames and create one visible/Details warning.
- Foreground token deltas are coalesced for React updates while Details retains the aggregated provider chunk count.

## Motion

The shared motion layer is CSS-only with no animation-library dependency. Keyframes and utilities are centralized in `app/globals.css`; `html[data-motion="off"]` remains the deterministic test switch. There is no page-load shell entrance animation.

Sanctioned moments — no decorative motion outside this list:

1. Run lifecycle: the evidence-based pipeline indicator's live activity glyph carries a working pulse (`pipeline-pulse`, 1.3s opacity breathing); idle and settled pipeline chrome do not animate. The completed turn's metadata line cools from the proof accent once (`settle-flash`, 800ms via `data-run-settled`).
2. Streaming liveness: only the active progress dot and answer caret pulse. The answer text itself remains visually stable, so coalesced token flushes update the live row without restarting a whole-block animation and memoized historical rows never repaint.
3. Popover/menu/dialog entrances: every picker, action menu, dialog, command palette, and mobile drawer uses the shared `pop-enter` utility (120ms ease-out, opacity plus ~2% scale). Exits stay instant via unmount — no exit animations.

Rules:

- One-shot motion stays within 100-350ms, ease-out; continuous liveness animations are opacity-only breathing at >=1.3s periods.
- The pipeline indicator derives from existing client-side run state only; adding backend stage events for motion is out of contract.
- Adjacent token events aggregate into one bounded client timeline entry, and workspace summaries structurally contain no message content, so token-only thread-cache updates do not repaint navigation. No per-token style mutation occurs outside the streaming row; historical row memoization stays intact.
- Skeleton shimmer (loading placeholders) predates this list and keeps its own keyframes.
- New motion requires extending this section first.

## Interaction Contract

### Composer

- Enter sends.
- Enter does not send while an IME composition is active. One client-safe guard recognizes native/React composing state, `Process`, and legacy keyCode 229 across the composer, command palette, shared pickers, and inline chat/folder create/rename handlers; composed Enter/Escape never commits or cancels partial text.
- Shift+Enter inserts newline.
- The visible `Send` action keeps the stable label `Send`. It is disabled while streaming, while a blank workspace is creating its first persisted chat, or while empty with no staged attachments. In edit-branch mode, attachments alone are not sendable: edited text is required, and `Cancel edit` remains available in the branch strip.
- Composer input stays disabled until catalog and workspace/chat-detail bootstrap complete and whenever the current user has no entitled model. Loading, no-model, creation, and actionable disabled reasons render as status text in the composer frame.
- Send is disabled while uploads are in flight so a message cannot silently skip a staged file. The textarea remains draftable, the frame reports `Uploading…` as status/busy feedback, and file selection/drop is unavailable until the upload settles.
- The visible `Stop` action remains available while the active chat streams. It stays disabled with `Starting run…` until the client has the persisted model-run id needed to call the server cancel endpoint. Stop requests durable cancellation before aborting the browser stream or painting a cancelled tail; a losing cancel reconciles the returned current status instead of fabricating local cancellation.
- When another chat's response is still running, the current chat's composer can draft and send its own run; only the chat that already has an active stream is blocked from sending another run.
- Switching away from and back to a chat with an in-flight foreground stream preserves optimistic user/assistant rows and cached token deltas until the final `chat_update` canonicalizes the thread.
- Draft text, staged attachments, edit intent, pending send/upload generations, and operation feedback belong to one keyed in-memory composer session: a saved chat, the blank root, or a specific blank folder. Navigation only selects that key; it does not copy composer content through a singular active buffer.
- Reload/background-run resume polling distinguishes a found run, proven 404/not-found, and transiently unknown network/non-2xx/malformed reads. Unknown reads retain only that source chat's run gate and continue bounded backoff; at the horizon the gate remains until `Check run`, cancellation, navigation-owned exit, or a later proven terminal read, while other chats remain sendable.
- Refetched model-run Details state is always written to its explicit source chat key, including after navigation. The active-chat selector alone chooses what is rendered, so a slow run GET cannot paint stale events or usage into another chat and the source remains inspectable when revisited.
- Textarea auto-grows to 200px max.
- The whole composer is centered and capped at `max-w-reading` (46rem). Edit/disabled strips, attachments, textarea, controls, context/usage, and actions all stay inside the same frame rather than stretching independently across the thread pane.
- A ready blank workspace centers that one composer and its short orientation copy in the available conversation stage. Its prompt-first variant hides empty-conversation Share/Details/menu actions, visually suppresses the redundant Message label and Usage line, and keeps the same direct Model/Profile/Search plus More controls without changing their underlying owner. As soon as first-chat creation begins or any optimistic/persisted message exists, the same mounted composer moves to the thread tail and the normal conversation actions return. Loading, error, no-model, saved-empty, and active/history states keep their existing truthful bottom placement; there is never a second composer or draft owner.
- The draft has one distinct bounded `Message` plane at every viewport. `Ask AIQSA…` remains visible guidance; the field does not acquire a second compact or reading-mode presentation when the user scrolls.
- The normal thread-tail footer contains direct Model, configured Fast/Balanced/Deep, and Search controls, a More action for the complete Run setup, a labeled Tools entry, one Usage disclosure, Attach, and Send/addressable Stop. The centered blank variant keeps those same owners and omits the separate Usage line until the conversation starts. The direct controls wrap as space narrows rather than disappearing behind More. Approximate current input, safe input budget, total model context, complete context, and matching active-branch/provider-reported usage remain in the existing Usage disclosure rather than becoming a second resting status line.
- Draft text, attachments/uploads, edit work, actionable errors/unavailable states, and streaming cancellation all render within that same composer. Stop remains visible while streaming and stays disabled with `Starting run…` until the persisted run id exists.
- Attachment chips support uploaded PDFs, images, and text-like documents. Text-like documents remain available to every selected catalog model because they become extracted provider text; PDF availability follows `documentInputMode`, and images follow `imageInput`. Picker/drop acceptance uses that matrix, and switching models removes newly unsupported staged files with source-composer feedback instead of leaving an invalid send hidden in the draft. Each upload batch captures its source key and generation before awaiting; files settle independently so a failed item does not block later items, successes accumulate only at the source, and mixed-selection capability rejections compose with upload failures without being erased by a successful sibling or leaking into another chat/later batch.
- Send atomically snapshots and clears its source draft/attachments before the first await, while the store prevents a send and upload from starting over each other. Success or cancellation retires that snapshot; failure restores it only if the source has no newer draft/attachment revision, so sent attachments cannot leak into the next question and newer work always wins.
- Entering edit mode snapshots the source composer's unsent draft without touching staged attachments. Cancel restores that exact draft; successful edit retires it. Each keyed composer session owns at most one pending edit PATCH: repeated submit, Cancel, and edit replacement are disabled only for that source, while another chat may edit/send/run independently. Settlement remains fenced by source key, message id, revision, pending owner, and generation; success activates the canonical refreshed branch (or the decoded committed response if detail refresh is unavailable), failure leaves a retryable edit, and stale/cross-chat responses cannot clear newer input or scroll another chat.
- A blank root/folder session transfers atomically to the chat created by its first send. Creation or send failure restores the captured content under exactly one session when no newer work replaced it; a successful send retires only its pending snapshot and never clears a newer draft.
- Deleting a folder removes its blank-folder composer session, discards late upload results, and returns an active blank-folder composer to blank root; saved chats moved out of that folder keep their own sessions.
- The attachment picker accepts multiple files in one selection, including PDF/image files plus `.txt`, `.md`, `.markdown`, `.csv`, `.json`, `.html`, and `.htm`.
- The catalog `documentInputMode` distinguishes `native_pdf`, `pdf_text_extraction`, and `none`; the composer uses the backend-filtered model catalog for attachment eligibility instead of guessing from provider names.
- The composer input frame accepts dropped files from the same attachment set and passes them through the same upload path as the paperclip picker. File drags get a quiet proof-accent border/ring while over the frame. Non-file text drops are not intercepted. File drops while disabled, streaming, or already uploading are prevented from navigating away but do not start another upload.

### Command palette

- Ctrl/Cmd+K opens.
- Ctrl/Cmd+K is ignored while focus is inside text inputs, textareas, selects, or contenteditable regions.
- Escape closes.
- Arrow up/down wraps through results; Home/End moves to the first/last result.
- Enter executes selected item.
- Filtering covers actions, models, search strategies, prompt presets, and the full workspace chat inventory, including provider/category aliases and non-presentational catalog ids in the model search index. A sidebar title/content query filters only workspace navigation and cannot remove chats from command-palette search.
- Results are grouped under readable Actions, Chats, Models, Prompts, and Search strategies headings with group counts. Current chat/model/prompt/search rows use a visible `Current` marker; category and readable provider/capability/search context form the secondary line instead of exposing raw ids.
- Long labels wrap, results scroll locally, keyboard movement keeps the active row visible, and an unmatched query replaces stale results with a readable `No matching commands` status. Enter does nothing when there is no result.
- The palette is viewport bounded. Arrow/Home/End/Enter operate its current option, and Escape or the backdrop closes it.
- Settings can be opened from Account at every viewport, the command palette, or `Manage prompts` in composer Run settings.
- The command palette includes a `New chat` action for keyboard and mobile users.

### Left pane

- The header gives the proof-backed primary action to `New Chat`, keeps `New folder` secondary, and places one labeled search field immediately below them. `New Chat` remains the pane's first focusable action.
- Chat search filters lightweight loaded summaries locally by title/provider/model only. Loaded message bodies do not become an accidental second local-content index.
- Non-empty search text also debounces a `/api/chats?q=<query>` request and merges server-side message-content matches, so chats that have never been opened in the current session can appear. The pane distinguishes `Title match`, `Model match`, and `Message match`, announces result/message-match counts, waits before showing empty results, and reports message-search failure while leaving local title/model results usable.
- Search results retain matching ancestor folders and temporarily reveal matches under locally collapsed branches. Clearing search restores the user's stored collapse choices and scrolls the current chat back into view when available.
- Chats with no folder render as plain top-level rows without a synthetic folder header. `No folder` is the row-menu destination label and explanatory copy calls this the `top level`. Folder rows use bounded indentation for nesting, total visible descendant-chat counts, a project-memory cue, and an active-descendant cue; collapsing a folder hides its whole descendant branch without making the active location ambiguous.
- Chat titles may use two lines and always retain the complete value as a tooltip. Provider/model plus update-date metadata appears only for duplicate-looking titles, model-query matches, or chats whose saved model is no longer available for new runs. An unavailable chat remains readable/switchable; archived chats are removed from the active workspace response and therefore do not render as disabled rows.
- Selected chats have one visible selected state; favorites use one quiet star cue; every in-flight chat id from `runLifecycleStore.activeStreams` gets a readable live indicator even while another chat is open. Favorites still sort before non-favorites, and each group stays newest-first within that split.
- Each chat reserves exactly one overflow affordance instead of a permanent delete/favorite icon toolbar. Its named popover contains Favorite, Rename, Move, Share, Export, and confirmation-gated Delete; folder popovers contain New chat, New subfolder, Project settings, Rename, Move, and confirmation-gated Delete. Popovers scroll locally, bring themselves into the navigation viewport, and close on Escape/outside click without activating the row beneath them.
- All confirmation flows use shell dialogs with Escape/backdrop cancel and appropriate destructive or warning styling; native browser `window.confirm`, `window.alert`, and `window.prompt` are not used for app confirmations. This includes chat, folder, message, and prompt deletion plus discard-unsaved-changes prompts.
- `New Chat` opens a blank workspace with no folder and without creating a persisted chat row. Folder-menu `New chat` opens a blank workspace with that pending folder, and persistence happens only on the first send.
- The compact top-rail `Start new chat` is another trigger for that same top-level blank-workspace action, not a second creation path; it preserves keyed drafts and does not stop an active run in the chat being left.
- Account provisioning creates no synthetic default folder. New chats stay at the top level unless the user explicitly starts or moves them inside a user-created folder; existing folders remain ordinary user-owned workspace data.
- The mobile workspace drawer button is the leftmost top-rail control, using the same left-pane icon language and leaving the pipeline and right-side Details controls separated.
- Folder creation hides behind a `New folder` header action that reveals an inline name input on demand; Enter creates, Escape cancels, and the input is not permanently visible.

### Provider/model/prompt/search controls

- options come from the backend-filtered current-user catalog;
- direct Model, configured Fast/Balanced/Deep, and Search controls are present at every viewport, with **More** opening the complete Run setup. They wrap within the composer at compact widths and use the same control actions and state owner as the expanded setup;
- the direct Model action and the model editor in More open the same searchable grouped picker behavior; selecting a concrete model from another provider applies provider and model atomically;
- configured Fast, Balanced, and Deep profiles render as direct one-click actions and also remain at the start of Run setup when at least one administrator-enabled slot exists. They use the concrete opaque deployment plus reasoning mode/effort projected by the current-user catalog; their adopted defaults are Luna / Standard / Medium, Terra / Standard / Medium, and Sol / Pro / Maximum, but the browser contains no family/upstream-model lookup table. A profile applies concrete model plus reasoning atomically, restores that model's non-profile draft, and never silently downgrades an unavailable mode, effort, or model; the model picker remains the searchable manual concrete-model selector;
- profile selection versus Custom setup is derived only from the effective opaque connection/deployment/reasoning mode/effort tuple and is never persisted separately. Search, prompt, temperature, output limit, background, and Stream remain independent controls. An enabled target unavailable under the current user's entitlement/runtime catalog stays visibly disabled with a generic reason and no leaked model identity; administratively disabled slots are absent, and the entire group is absent only when every slot is disabled;
- model picker provider group headers are noninteractive headings with model counts; only a concrete model row is focusable/selectable, and selecting it applies its provider atomically;
- Search is a direct labeled picker; Reasoning remains in the complete setup one More action away and is applied atomically by a selected profile;
- Run setup is a modal bottom sheet on narrow screens and a centered viewport-bounded modal on wider screens, with a scrim, explicit Close, safe-area padding, and one locally scrolling content owner. Profile, Model, Reasoning, and Search precede the complete advanced inventory. A nested model picker closes back to Run setup without resetting it. Closing Run setup flushes numeric drafts and returns focus/state to the direct controls; Ctrl/Cmd+K stays blocked while the modal owns the layer;
- the shared advanced inventory contains prompt preset plus `Manage prompts`, temperature, max output tokens, model-specific reasoning mode, background mode, provider Stream response, citation visibility, reasoning display, tool-activity display, and answer sound. Mode appears only when the backend catalog advertises it, and unsupported capabilities stay disabled or absent;
- response/display/sound switches use full readable labels with On/Off state; an in-flight answer reads the latest answer-sound preference when it actually completes, so changing the switch during a run affects that run's terminal notification;
- provider/model/search/prompt changes affect the next message only and deliberate changes are autosaved through current-user settings;
- users with no entitled catalog models see a disabled `No models available` composer control and a model-access hint, not a saved/default provider-model pair;
- search strategy changes for a selected model save only that model's run-control draft, not a global search fallback, so each model can keep its own supported search mode;
- per-model run-control drafts save search strategy, background mode, Stream mode, reasoning effort, reasoning mode when supported, temperature, and max output tokens so each model can keep its own supported run shape;
- send and regenerate payloads include the current per-model `controlDefaults` draft; the backend persists that validated draft with the run so a reload immediately after Send does not depend on a separate in-flight settings autosave;
- attachment-only sends use structured optimistic user-message content so staged image/file/PDF/text document chips are visible immediately instead of waiting for a canonical chat update;
- typed temperature and max-token changes are debounced before saving, with blur, Enter, Run setup close, or shell unmount flushing the pending save; same-model immediate background/Stream/reasoning toggles merge and clear pending numeric drafts so late debounce flushes cannot clobber newer toggles;
- current-user settings persistence has one shell-lifetime coordinator and at most one PATCH in flight. Pending scalar fields use latest-intent semantics, pending model drafts deep-coalesce by model key, and each decoded response reconciles only the fields its request touched before overlaying newer queued intent. HTTP/network/malformed-success failure keeps one recoverable pending patch behind a visible Retry action; a later settings intent also retries that retained work, while page-hide delivery remains best effort;
- model switches, chat activation, and page reloads restore a model's saved search/reasoning/control draft when valid and clamp stale search drafts to that model's backend-filtered strategy list;
- citation, reasoning-block, and tool-activity visibility toggles persist as current-user presentation defaults. Tool-activity visibility defaults on and hides only the inline disclosure; the durable calls and Details/Events evidence remain available and execution is unchanged;
- OpenAI, Gemini, and OpenRouter expose the Stream response setting when the current model's catalog controls support user selection; OpenAI defaults off, Gemini/OpenRouter default on, and providers without Stream-control support can still stream through their adapter defaults.
- model selection is searchable and grouped by provider;
- model search matches provider display name/family, model display name/upstream id, and readable capability text without rendering internal connection or deployment ids. Provider sections remain visible as noninteractive headings with model counts and optional current-group context;
- the model picker clears its search query after a model selection, closes if another surface such as Command palette changes the selected model while it is open, and renders a dedicated zero-match status instead of an empty or stale list;
- model rows wrap long display names and use readable capability text such as `Reasoning`, `PDF and documents`, `Web search`, and `Streaming`, never internal ids or a row of tiny uppercase chips. Capabilities that distinguish a model inside its provider group appear first with normal emphasis while shared capabilities remain readable but quieter; the complete inventory is retained. Command-palette results use the same plain vocabulary while technical ids and aliases remain search-only; `Current` and catalog `Default` are independent row markers;
- prompt selection uses a searchable picker inside Run setup; prompt management is a separate labeled action in the same owning surface. Prompt search matches preset names and instruction text, clears on every close/selection, and owns a readable no-results status;
- prompt picker rows wrap the preset name, show up to two lines of system/developer-prompt preview or `No prompt instructions`, and distinguish `Current` from the deletion-protected `Default`; no raw preset UUIDs appear anywhere in the shell UI;
- Search strategy and other finite option pickers share the same readable row recipe: label, meaningful description, independent `Current`/`Default` markers, bounded local scrolling, and a dedicated unavailable-options state. Search opens directly from the composer and remains editable in Run setup without gaining a redundant query field;
- model, prompt, search-strategy, and finite-option picker rows support pointer selection plus Arrow up/down, Home/End, and Enter navigation. Escape and outside click close the session;
- picker surfaces have purposeful desktop widths and local maximum heights. On narrow or short-height screens they become safe-area-aware bottom sheets with contained scrolling, wrapping labels, and capability-driven touch-sized rows instead of clipping outside the viewport;
- internal provider/model ids never appear in ordinary UI. Upstream API ids remain available only where they carry configuration or diagnostic meaning, such as the OpenRouter discovery picker, compatible-provider fields, Advanced model settings, and deep API inspection; routine model rows, answer metadata, chat metadata, grants, usage, and unavailable fallbacks use display names or neutral copy;
- model display names do not repeat the selected provider in suffixes such as `via OpenRouter`; the compact composer trigger keeps provider context in its tooltip, while the expanded picker groups models under visible provider headings;
- prompt presets can be created, duplicated, updated, deleted when not the current default, selected for the next run, and used as defaults for new chats;
- Escape and outside click close picker/popover and left-pane action-menu sessions where safe.

### Run receipt

- Every non-streaming assistant answer gets one quiet compact evidence block derived only from that message's status, stored provider/model identity, bounded search/citation/artifact summary, message-bound warnings, and provider-reported usage from a terminal `PersistedRun` whose id exactly equals the message `runId`. Search and citation facts expand their existing details inside that block instead of rendering as separate stacked summaries. It does not reconstruct historical Profile, Search, or other facts from the current composer/catalog defaults and adds no fetch, store, API, or inspection tab.
- Search, Tools, Citations, and Reasoning segments open the existing disclosure on their originating answer when that disclosure is rendered. A hidden evidence segment or status/model/usage/context/warning segment opens Details → Events only when the same answer owns the exact currently loaded persisted run and that run has real events. Otherwise the segment remains factual noninteractive text; a historical answer must never open another answer's Events. The receipt is absent while the answer streams.

### Details pane

- new sessions start closed; explicit Pin is the only open presentation persisted across reloads, and it restores only at the `>=1440px` eligibility breakpoint;
- run start/completion and fork activation never mutate Details presentation or steal focus;
- the full Details surface shows exactly Branch and Events, in that order;
- Details tabs implement one controlled roving tab stop with Arrow/Home/End navigation, visible focus, wraparound, and stable tab/panel relationships;
- the Details header shows readable active/error/run status and an Overlay/Pinned mode label; errors are wrapped text, not icon-only chips;
- Close removes Details; Overlay does not alter the grid; Pin adds the wide-only 360px column; Unpin keeps Details open as an overlay;
- the `Branch tree` item in `Conversation actions` opens Details on Branch at every viewport;
- readable live run activity opens Details on Events; no activity control is rendered while idle or settled;
- Run setup is the only UI editor for temperature and max-output drafts. Closing Details has no parameter-flush side effect because Details owns no drafts;
- overlay Details uses one shared responsive state and selector on desktop/mobile, with modal focus/background behavior only while overlaid;
- Details never presents next-run draft values as historical request inspection. Historical/deep request inspection stays in Events and model-run APIs; a future immutable Request tab requires a separate persisted data contract.
- More opens the Run setup that owns exact reasoning mode/effort, background, Stream, and the advanced inventory. Those controls appear only when the selected model's backend catalog advertises them; citations, reasoning display, tool-activity display, and answer sound remain user-facing preferences there.
- Events is a chronological digest with ordinal/stage cues and semantic status glyphs. Provider/search/tool/citation/reasoning/token/usage noise is aggregated in place at each category's first occurrence, so later counts/details update without moving rows. Warning, long failure detail, cancellation, and successful completion remain distinct and fully wrapped; raw token deltas and internal assistant ids never render.
- empty Events states explain that events appear during a run and that a run never opens Details automatically;
- Branch renders readable Q/A role labels and plaintext multiline excerpts: stored Markdown remains unchanged, linear chains stay flat, depth increases only below actual fork points, fork choices expose the user-facing `Open this version` action, and the active leaf is named without exposing message ids;
- Branch empty/linear guidance explains that edit, regenerate, and Branch from here create branches;
- thread messages show calm, collapsed search/tool disclosures after search artifacts exist, using count-first wording such as `3 search calls`, revealing captured request/response previews when requested, falling back to OpenAI `web_search_call` action/source records or explicit metadata-only wording, and showing a no-preview fallback only when neither details nor call records were captured;
- when `Show tool activity` is on, each assistant run with observed client-executed calls shows a default-collapsed summary during execution and after reload. Expansion groups calls by model round, marks same-round batches as parallel, and gives each call a disclosure with MCP server/tool identity, live or terminal state, duration, safe account/credential-source metadata, and bounded redacted argument/result previews. Failed and cancelled calls remain explicit; no raw executable configuration, endpoint, secret, or unrestricted payload is rendered;
- thread messages show optional collapsed citation disclosures when provider/search citation artifacts exist, including direct Perplexity answers and historical Perplexity search runs; links remain safe and long titles, URLs, and snippets stay contained;
- thread messages show a compact context-window hint before the answer when the backend emits a `context_truncated` artifact for dropped prior turns;
- Events shows context-window truncation as a warning row with dropped message/token estimates;
- empty citation/reasoning artifact disclosures state that no text was captured only after the user expands them;
- Branch checkout rows are disabled while a response is streaming and expose a visible stop/finish-first explanation.
- reasoning/thinking artifacts can be toggled visible from the composer control bar; provider summary arrays render as readable text blocks and empty fragments are not counted when text blocks exist.

### Auth screen

- `/login` is one restrained, flat `max-w-[42rem]` auth workspace on the answer-paper canvas, not a marketing page, generic SaaS card, or tiny floating panel. Its identity header presents AIQSA and `Question → Search → Answer` as quiet product orientation; each sign-in, request-access, invite, verification, reset-request, reset-completion, and request-result mode owns the page's sole `<h1>`, orientation copy, primary action, and only the relevant secondary route.
- Normal sign-in shows neutral `Continue with Google` and/or `Continue with Yandex` links only when the server has both credentials for that provider. They preserve the same sanitized internal `next` destination and remain absent from invite, verification, and reset modes. OAuth cancellation, provider failure, disallowed/pending account state, or a conflicting provider binding returns to the clean login URL with one readable privacy-safe outcome; raw provider errors, codes, and tokens never render.
- First OAuth use attaches the external identity to the existing normalized-email `User`, so password and OAuth login reveal the same chats, groups, settings, and entitlements rather than parallel accounts. Google linking requires its verified email claim; Yandex linking uses the authenticated profile's default email under the server policy documented in `SECURITY.md`. Later sign-in follows the stable provider account id.
- When multiple proof parameters are present, active proof precedence is `verify` → `reset` → `invite`. Successful invite/verification/reset settlement and explicit return to ordinary sign-in retire that local proof and replace the current history entry without any consumed bearer parameter while preserving safe `next`, OAuth outcome, and fragment state. The same mounted view does not resurrect retired proof from unchanged props, but a genuinely new proof tuple activates its new mode. An initial OAuth callback outcome appears near the heading before the sign-in form so it remains visible on short/mobile viewports; mutation success/error feedback remains in document flow after its form.
- Sign-in, invite-acceptance, verification-password, and reset-password fields have a visible `Show password`/`Hide password` control. A normal access request collects no password; direct invite acceptance does. Toggling visibility never clears the value; switching modes returns it to concealed. Password help stays visible and the browser receives `current-password` versus `new-password` autocomplete semantics.
- Each mutation disables its active fields, visibility toggle, submit, and mode-navigation actions, and uses a specific working label/status such as `Signing in…` or `Updating password…`. The user cannot submit twice or replace the active mode while a request is pending.
- Local validation plus backend/network failures show readable sentences in an announced `role="alert"` while preserving the stable backend code in parentheses. A failed login, invite acceptance, allowlist check, SMTP configuration/send, verification, or reset attempt stays retryable in its current mode and preserves entered values where applicable.
- Normal access requests honor `verification_required` with a check-email result if a server supplies that status. The current server normally returns the intentionally generic `request_received` outcome, which never claims that an email was sent or reveals whether an account exists; allowlist denial, SMTP unavailability, and SMTP send failure remain on the form. Password-reset requests likewise say only that a link was sent if the account can reset.
- Invite links open `Create your account` directly and never render the raw token or ask the recipient to re-enter the invited email. The two-field form collects name plus a new password, submits them with the one-time token to direct invite acceptance, then uses the returned session cookie and safe `next` path to enter the workspace immediately; it has no check-email or second-sign-in state. Verification links remain the normal access-request path: they present the one-time new-password form, then distinguish active sign-in, pending-admin-approval, and generic invalid/error outcomes. Reset-completion links explain one-time use; success returns to sign-in and existing server behavior revokes prior sessions.
- The normal auth workspace never renders an access-token field, Bootstrap token action, recovery-route hint, or fake open-registration promise. Safe internal redirect normalization, route guards, account approval, verification, SMTP, and password policy remain server contracts rather than presentation choices.
- At 390x844 the workspace is top-aligned, uses `100dvh` plus safe-area padding, remains vertically scrollable when the software keyboard reduces space, keeps the current primary action reachable, and has no page-level horizontal overflow. At desktop widths through 720px height and at 844x390, Request access/invite registration use a compact two-field row followed by primary/secondary actions so the panel bottom, primary action, and `Back to sign in` are initially visible while labels/help remain intact and the page can still scroll. All approved palettes flow through semantic tokens and preserve browser-autofill contrast.

### Public share

- `/s/[shareToken]` remains anonymous and hashes the secret token before server lookup. The route passes only the stored public title and immutable sanitized snapshot into `PublicShareView`; missing, revoked, expired, or invalid links all use the same 404 `Shared snapshot unavailable` view with no reason, owner, token, or other private detail.
- A quiet workspace-rail header pairs AIQSA identity and `Shared research` context with an explicit `Read-only snapshot` marker. One wrapped `<h1>` names the shared chat, blank titles safely fall back to `Shared chat`, and one divided note explains that this fixed branch will neither update nor change the original.
- Shared questions use the private thread's compact right-aligned filled treatment; shared answers stay full-measure document flow on answer paper. There are no literal role labels, provider/model metadata, message actions, composer, Details, branch controls, share/revoke controls, calls to action, or authenticated-shell dependencies.
- Rendering consumes only the snapshot's user/assistant text blocks and the attachment-omission text produced by the existing sanitizer. Private message/share/run ids, attachment object ids/URLs, raw provider payloads, secrets, and user/group metadata are never read into the view. Safe links and citations already represented in sanitized Markdown remain usable; raw HTML and unsafe URLs remain inert through the shared `MarkdownMessage` renderer.
- An empty snapshot has a deliberate no-visible-messages state; an individual empty turn has neutral fallback text. Neither empty nor unavailable states invent content or add a call to action.
- The public page uses the shared reading measure and semantic palette tokens at every viewport. Long titles, prose, URLs, tables, and fenced code stay within the page; only table/code owners scroll horizontally. Code highlighting/copy and Markdown heading offset match the private thread, while the page itself stays overflow-free at 390x844.

### Admin screen

- `/admin` is the Control Center: a deliberately denser operational workspace using the same product-semantic surface hierarchy as Research Chat, not a consumer dashboard or chat clone. Its safe-area-aware `100dvh` canvas has one restrained identity/refresh header, grouped navigation, and the current destination. Loading or failed initial data shows a deliberate state instead of zero-valued metrics; refresh/save state is visible feedback and readable errors wrap without exposing local underscore codes.
- Navigation uses stable subject headings in this order: AI setup — `Providers`; Team & access — `Users`, `Access & groups`, `Invites`, `Access rules`; Operations — `Usage`; Infrastructure — `MCP servers`, `Email delivery`; Safety — `Safety`. Headings are orientation text, not plans, roles, entitlements, routes, or collapsible setup stages. Bare `/admin` selects Providers.
- The canonical section IDs are `providers`, `users`, `access`, `invites`, `access-rules`, `usage`, `mcp`, `email`, and `safety`. Legacy `groups` and `model-access` values normalize to `access`; unrelated query and fragment state survives section changes where the existing navigation owner permits it.
- Desktop uses one persistent vertical rail. Compact widths show one active task plus an `All sections` entry; the vertical section index is its own browser-history entry with an explicit Back action. There is no compact horizontal tab strip and no disclosure state derived from installation topology.
- Active admin app sessions expose a discreet Account-menu entry to Providers; non-admin sessions do not render a misleading admin entry.
- The Control Center header keeps identity, administrator email, a neutral `Return to chat` path, Refresh, last-update, and save/loading state. Global `Revoke all sessions` lives in Safety, not beside harmless header actions.
- There is no global KPI or attention strip and no Overview route. Pending users, missing access, open invites, and other actionable attention remain reachable in their owning destination. The initial resource unavailable state and last-good refresh behavior stay owned by `useAdminDashboardResource`.
- Users and Access & groups use full-width searchable/filterable indexes with no automatic first selection. The complete resource row opens one dedicated detail state; there is no small `Details` or `Review` discovery target and no permanently mounted desktop detail pane. Back returns to the preserved index. Other destinations may reuse this pattern when their task is resource-owned, but a universal split-pane template is not a product requirement.
- `Users` keeps status filters, sort/direction, count/pagination, group/access summaries and last-session facts in its index. User detail owns pending groups and Approve/Reject, memberships, effective entitlement inspection, direct MCP-server/personal-slot grants, stale-slot cleanup, deletion eligibility, and scoped account/session actions. The acting admin row is marked and self-destructive actions are not exposed through normal actions. The server still independently rejects self-disable and final-active-admin removal, and the console preserves the current view while surfacing those stable errors.
- Wide analytical or assignment comparisons keep a named local horizontal-scroll owner. They are never the compact information architecture: Usage switches to native factual rows, while resource selection and operations use separate index/detail task views. Long emails, names, group labels, explicit technical search ids, and errors wrap inside their owner rather than widening the page.
- The `Usage` destination is a read-only analytical document over provider-reported `UsageEvent` totals: total/input/cached/cache-write/output/reasoning/last-use facts, group attribution by current membership, and user runs/tokens/input-output/top-model/last-use facts. Compact widths use native factual rows rather than a squeezed table; desktop may use comparison tables. Failed/cancelled runs appear only when the provider reported usage before termination; token totals may retain older detached usage whose deleted run no longer contributes to run count. Group totals are attribution, not billing reconciliation, because users in multiple groups appear in each group.
- Pending approval is a focused user-detail workflow: verified identity is visible, default groups can be selected before approval, and rejection is destructive/confirmation-gated.
- `Access & groups` owns one group index and one group detail. Detail peer tasks are Overview, Members, Models & search, and Tools. Members adds/removes active users directly while submitting the user's complete preserved active-group set; administrators do not have to leave the group and rediscover membership in Users. Ordinary groups preserve rename/archive/delete, provider-wide and explicit model grants, search-strategy grants, MCP-server grants, and scoped clear actions. Archived groups remain inspectable but their grants do not apply and every membership/grant mutation control remains unavailable. Exactly one visible `Full access` row is marked `Built-in`: its first administrator membership is established by installation state, later membership stays explicitly editable, lifecycle actions are absent, and Models & search plus Tools explain automatic current/future coverage instead of exposing grant switches. New users are not auto-enrolled. Generated Full-access MCP grants are installation foundation and do not by themselves make a sole-administrator setup `teamConfigured`. Provider credential assignment and MCP personal/OAuth secret ownership stay separate concerns and never masquerade as entitlement.
- Providers opens on Quick setup and requests only `GET /api/admin/providers/quick-setup`. It shows OpenAI, Anthropic, Gemini, and OpenRouter, one empty write-only key field for the selected provider, and one **Test & Save** action. The compact form uses a 2 × 2 provider grid, the wide form uses one four-choice row, and both keep the key plus primary action in the first task viewport; the explanatory guide is large-desktop-only. Existing group assignments, extra credentials/models/grants, or custom sibling connections may be reported as nonblocking advanced context but never replace the form with an `Advanced` status. The acting administrator's direct credential assignment and model entitlements remain separate facts. The one synchronous request has the truthful pending label **Testing & saving…**; the browser does not simulate unseen phases. Only server-returned opaque reviewed candidates appear when `selection_required`; the retained local masked key is resubmitted with that choice and the server retests it. Ready names the selected default plus every reviewed current model made available, gives a factual receipt for the saved key, acting-administrator access, conditional default update and exact profile fills, offers **Start chatting**, and clears the secret. Retryable provider/stale failure retains the current form value, while provider change, Ready, Advanced disclosure, close, or unmount clears it. Replacement starts empty and targets the acting user's assigned credential. Before any write, the new key must expose every currently available canonical-connection model already entitled to that administrator (bounded to 64); the commit records a new exact check for each, so failure never silently removes model availability. Ready also offers confirmation-gated **Remove my key assignment**: it calls `DELETE /api/admin/providers/quick-setup`, removes only that actor's direct assignment, retains the stored credential/grants/defaults/team state, and warns that provider access may stop without an applicable group/default credential.
- Advanced is an explicit secondary subview over the same Providers destination, not another navigation group or an onboarding step. Quick does not mount `useAdminProvidersController` or `AdminRunProfilesPanel`, and it does not request `/api/admin/providers` or `/api/admin/run-profiles`, until the administrator opens Advanced. Returning to Quick fully unmounts Advanced and refetches the actor-relative Quick projection rather than keeping two live state owners.
- Advanced Providers is a task-led control plane with peer `Connections` and `Run profiles` destinations. Connections first use a full-width index; selecting one opens a dedicated full-width detail with Back navigation and focused `Credentials`, `Authentication policy`, `Models`, and `Diagnostics` tasks. It never keeps a persistent connection list beside the selected connection. Runtime `Enabled`/`Disabled`, publication `Not configured`/`Changes pending`/`Active`, key add/rotate/test/revoke, default/group/direct-user authentication assignment, account-filtered OpenRouter discovery/routing, optional paid diagnostics, compatible-protocol choice and exact server-authoritative activation retain their original consequences. A never-activated `Not configured` connection uses neutral setup guidance; warning color/count copy is reserved for an already configured or pending state that genuinely needs attention. Credential assignment selects a key and never grants model entitlement.
- `Run profiles` stays in fixed Fast/Balanced/Deep order as a peer Advanced task. The matrix edits description, one concrete active deployment, and advertised reasoning mode/effort; one Save submits all three versioned rows atomically. Quick setup never mounts this editor and may populate at most one provably untouched slot only through the exact code-owned OpenAI recipe. It grants direct access to every reviewed candidate installed by the transaction but does not infer Anthropic/Gemini/OpenRouter profile recipes or change unrelated Search/temperature/output/Background/Stream defaults.
- `MCP servers` is the sole installation definition editor and uses catalog/detail navigation plus nested `Overview`, `Definition`, `Validate & tools`, `Revisions`, `Runtime`, and destructive tasks. `New server` opens an import-first form for one URL, common JSON configuration, recognized package/container command, or bare-command JSON followed by its recognized install command; normalization never executes pasted shell text, and the full remote/npm/PyPI/OCI editor is the secondary manual path. The official hosted Notion URL is recognized from either a direct paste or its one-entry `mcpServers` JSON and prepares per-user OAuth with the MCP endpoint origin already allowed; selecting OAuth manually for any remote endpoint likewise pre-fills that origin. A separately hosted authorization server remains an explicit reviewed origin. Saving a non-OAuth server immediately runs its initial update resolution/draft test, while successful administrator validation OAuth tests and activates the exact flow-bound draft automatically. Later edits and update candidates retain explicit review/activation controls alongside best-effort rollback/rebuild, reversible enable/disable, confirmation-gated irreversible Delete, and validation-OAuth actions. The activation action is absent when the complete tested revision identity is already active, while a newly resolved package artifact under the same draft/selector remains activatable. Delete removes the row from the live catalog immediately; accepted runs may drain and retain their immutable evidence, but there is no Archive/restore product workflow. Validation evidence and tools are informational until activation. Revision history distinguishes available, known-missing, not-applicable, and not-yet-verified artifacts; a known-missing revision directs the administrator to `Rebuild and activate` instead of offering a rollback that cannot start. Local-source copy states that the workload has unrestricted outbound networking and its effective environment is visible to trusted Docker-host administrators; activation states that every valid tool from the server is granted as one trust unit. Classified startup guidance may name a missing environment variable, but raw MCP stdout/stderr is never rendered.
- `Email delivery` owns the single installation SMTP draft and active snapshot through `Overview`, `Draft configuration`, `Test & activate`, `Runtime & health`, and `Clear configuration` tasks. Draft, active, and health are separate factual axes. Passwords remain write-only; Save, one-use-recipient Test, and Activate are explicit version-bound steps; current delivery health never exposes message content or raw SMTP replies. Plaintext SMTP requires a separate ephemeral exact-relay acknowledgement after every draft edit and is intended only for an explicitly trusted internal relay.
- `Invites` uses list/detail navigation for one-off invite creation, email delivery selected by default with an explicit link-only opt-out, fresh invite-link copy feedback, status/search filters, default-group display, detail-scoped revoke for open invites, and guarded deletion for stale revoked/expired invites. It reports sent/not-requested/unavailable/failed delivery truthfully. Every creation outcome keeps the fresh URL visible for manual copy, and the UI states that it cannot be recovered later because stored tokens are hashes.
- `Access rules` uses list/detail navigation for exact email/domain approval policy with a hidden-until-needed create form, normalized match preview, search, default groups, and detail-scoped confirmation-gated deletion. It explicitly distinguishes durable approval rules from one-off invites.
- The `Safety` tab isolates high-risk global session revocation and explains that it may include the acting admin's current session; user-level session revocation remains scoped to user rows/details.
- Existing destructive and warning confirmation behavior remains above the isolated admin workspace and names the affected account/group/invite/session scope. At 390x844 and 844x390, the section index and active task are separate compositions, Usage reads without horizontal dragging, necessary local comparison scrollers stay bounded, and the page itself remains overflow-free.
- Active admins can scan/filter users, select default groups for manual approval, approve/reject pending users, disable active users, revoke user/all sessions, delete stale users with no owned app data, create/rename/archive/delete empty groups, toggle group provider/model/search/MCP grants, manage provider connections/keys/models/group-key assignments and the three run-profile mappings, manage installation MCP revisions and direct-user/personal-slot grants, create/delete exact email/domain rules, create one-off invites, revoke open invites, and delete stale revoked/expired invites.
- Non-admin signed-in users see an access-denied state and the client dashboard does not request `/api/admin`, `/api/admin/providers`, `/api/admin/run-profiles`, or `/api/admin/email`.

### Settings

- The global Settings modal contains Prompts, Appearance, and `MCP & tools`. On desktop it is a bounded secondary workspace; at 390x844 and short-height viewports it becomes a dynamic-height, safe-area-aware sheet with locally scrolling content. Coarse/no-hover controls remain touch-safe at tablet and landscape widths.
- `MCP & tools` lists only enabled installation definitions covered by the current user's direct/group server grant and active revision. Each server has an independent persistent enable switch, readable readiness/error, current runtime-scoped tool names, exact administrator-declared personal fields, and OAuth Connect/Reconnect/Disconnect plus a safe account/workspace label when configured. A disconnected OAuth server presents `Connect … to enable` instead of sending an invalid enable mutation; successful OAuth completion turns on that server preference without a second click. Sensitive fields are write-only and clear after save; the user never sees or edits endpoint, package, command, environment/header target, OAuth scope/client policy, or tool grants.
- Several entitled MCP servers can be enabled together. A concise warning remains visible that conversation-derived data may reach an enabled tool; cross-server influence, ready-tool semantics, limits, and schema/context preflight live in the collapsed `How tools use data` disclosure so the server list enters compact viewports sooner. Before enabling another server, the client sums the known discovered inventories and refuses an already-known total above the 128-tool run limit; the server remains authoritative for exact schemas, byte budget, freshness, and races at Send. Readiness refreshes on section entry, relevant mutation/OAuth return, visible-tab polling, and normal authenticated runtime reconciliation; OAuth success confirms connection plus enable settlement, while `Ready` still comes only from runtime evidence.
- The composer always renders one compact `Tools` entry that opens `MCP & tools`; its title reports truthful `Not configured`, `Off`, ready/tool counts, and issue/limit state. Persistent enablement is not duplicated in each run or composer control.
- Prompts has one clear editor/library hierarchy: desktop gives the editor deliberate space beside the library; compact and short layouts show separate library and editor task views. The library owns `New prompt` and selection, a selected/new action opens the editor, and `Back to prompts` returns without losing the library's selection or scroll context. The editor owns name, required system instructions, optional developer instructions, and help text; the library keeps long names and a bounded instruction preview readable and exposes create, edit, duplicate, delete, `Use for next run`, and `Make default` as labeled actions.
- Editor selection is not run selection. `Next run` is the current composer choice for the next message only; `User default` is the persisted startup choice for new chats. Browsing or editing a preset changes neither, `Use for next run` changes only the former, and `Make default` changes only the latter.
- The editor derives `Saved`, new, dirty, invalid, and saving presentation from the shared prompt-settings draft. Name and system instructions are required; their Required metadata stays neutral while valid and uses the semantic critical/error token only with the associated missing-field error. Save/create is enabled only for a dirty valid draft while no mutation is pending, and fields/actions expose the pending state without losing their labels.
- Create, update, duplicate, and delete mutations use the prompt APIs. `Make default` uses the same serialized current-user settings coordinator as composer prompt selection, so two prompt-default intents cannot commit or reconcile out of order. Success keeps the optimistic catalog aligned with the decoded settings response; a failed default remains queued behind the Settings-scoped Retry action, while other prompt failures leave the catalog, next-run choice, and dirty editor intact and report a readable, dismissible notice below the section tabs.
- Prompts distinguishes catalog loading/error from a genuinely empty library. Until the catalog is ready it mounts no editor or prompt mutation action, offers resource Retry on failure, and keeps Appearance usable. A prompt mutation error scrolls the editor’s save/action region into the nearest visible position without discarding the draft.
- Dirty prompt edits cannot be silently replaced by close, Escape, backdrop, section change, New, another editor selection, or Duplicate. Those paths use the shared discard confirmation; deleting the dirty preset discards first and then enters the separate destructive delete confirmation.
- Unsaved personal MCP values use the same close/section discard protection. Once a server mutation or personal-value save starts, Settings blocks close and section replacement until that exact request settles; a confirmed discard cannot leave a supposedly discarded write running in the background.
- The user-default prompt is visibly protected from deletion. Changing the default moves both the marker and protection; deleting a confirmed non-default prompt falls editor/next-run state back to the surviving default when necessary, while a failed delete preserves the prompt and draft.
- Settings blocks the shell while open, closes through explicit safe Escape/backdrop paths, and keeps a current notice in the dialog flow. A nested discard or delete confirmation resolves before Settings resumes. Any action that replaces one modal workspace with another closes the current session before opening the next.
- Settings > Appearance renders AIQSA (`aiqsa`, dark), Graphite (`graphite`, dark), Verdant (`verdant`, dark), Classic Dark (`classic-dark`, dark), Classic Light (`neutral`, light), and Paper (`paper`, light) in that registry order. Paper provides a soft monochrome, graphite-action treatment inspired by familiar conversation-product restraint without copying brand chrome. Every choice applies immediately. All widths use one calm divided comparison list with a compact semantic preview, description, and current selection; it never becomes a dashboard-like card grid.
- Theme selection is a local UI preference only: it persists in LocalStorage, mirrors to the same-site `aiqsa.theme` cookie, and the server validates that cookie to emit matching first-paint `data-theme` and `data-color-scheme`. `neutral` is the first-use fallback. On hydration a valid LocalStorage choice wins a stale cookie and repairs its mirror; malformed LocalStorage falls back to the already validated server-rendered theme and is repaired rather than forcing `neutral`. Runtime switching updates both attributes together so browser controls and rendered content agree. It is not synced to the user account, prompts, or conversations.
- Project settings labels its sole context field `Project instructions` and explains that the text is supplied to chats in that project. It retains modal focus, Escape, opener-restoration, and dirty-confirmation contracts; an inner confirmation owns focus and Escape before returning to the project dialog.

### Messages

- private user/assistant articles are named `Question`/`Answer`; repeated turn actions reference a short target description so their destination remains unambiguous;
- user and assistant edits fork same-role branch replacements without mutating descendants;
- the edit strip states that sending creates a new branch;
- failed edit branch mutations keep source-keyed feedback and preserve that session's draft/editing state; one pending edit owner suppresses duplicate PATCHes without disabling another chat;
- assistant regenerate forks a sibling assistant run;
- regenerate is disabled while a response is streaming;
- message actions sit below each message body, preserve the order Regenerate/Edit/Copy/Delete before `Branch from here`, and keep stable reserved height so hover/focus reveal does not shift the thread;
- generic `New Chat` creates a no-folder blank workspace and does not persist a chat until the first send; only explicit folder actions create a first message inside a folder;
- a valid explicit Send in the still-active chat moves that thread to its optimistic newest user turn exactly once; later answer growth preserves the user-turn reading anchor rather than following the tail, while source-mismatched/background work cannot scroll the selected chat;
- send/regenerate optimistic rows, ids, tokens, and status patch only the source chat's keyed thread snapshot, including while another chat is visible; transient terminal `chat_update` patches that snapshot plus its lightweight left-pane summary through separate projections. A pre-accept HTTP rejection removes only its owned temporary rows and restores the previous leaf; accepted or ambiguous network/SSE failure force-refreshes only the source, and retry refuses to post from a stranded temporary id until that refresh succeeds. Lazy detail fetches are otherwise reserved for uncached/incomplete activation, recovery, export, and fallback paths;
- Branch tab checkout changes the same chat's active leaf and future sends continue from that selected branch. Pending checkout writes serialize per chat; Send waits for the selected leaf to settle, carries that exact nullable leaf to the server, and restores its draft without issuing a second checkout or run request when settlement fails or is superseded;
- deleting a message asks through a shell confirmation dialog, then removes that message and its descendants so the message DAG remains valid;
- deleting a chat from the left-pane row archives that chat and moves the UI to another chat or blank workspace;
- deleting a chat removes it with a functional chat-list update so concurrent stream/list patches are preserved;
- `Branch from here` clones the selected message's visible ancestor path into a new chat;
- `Copy message` copies only the selected visible message text;
- `Copy thread` copies the visible active branch text;
- assistant answers show subtle provider/model metadata in the thread;
- user questions and assistant answers share the composer's `max-w-reading` alignment; questions stay narrower/right-aligned while answers use the full readable document measure;
- completed assistant answers leave extra bottom scroll space so the final answer can sit comfortably above the composer; cancelled and failed tails keep a smaller terminal spacer;
- provider labels in thread and workspace rows use display names where available instead of raw provider ids;
- main thread and branch labels do not display raw message UUIDs;
- branch controls change active leaf and the thread renders the active branch path;
- provider/search/malformed-frame warnings carry their originating run id and render only beside the assistant message with that run id, so branch checkout cannot move them to an unrelated answer and returning to the source branch restores their placement;
- `Share (anonymously)` creates a sanitized secret-link snapshot of the visible branch, copies the link to the clipboard when possible, and keeps the exact link plus explicit `Copy link` and `Revoke link` actions visible in a persistent notice. Copy reports success/failure without hiding the URL or revoke recovery; create/copy/revoke use guarded generation ownership so late clipboard completion cannot resurrect or overwrite a revoking/revoked notice. Sharing busy state affects only share controls, never composer/send/streams; dismissed or reloaded links are not listed by a share-history UI.

### Markdown

- inline `\\(...\\)` / conservative `$...$` and display `\\[...\\]` / `$$...$$` formulas render lazily through the restricted local KaTeX boundary; incomplete, malformed, unsupported, or refused TeX remains readable escaped source;
- display formulas own a named, focus-visible local horizontal-scroll region and never widen the answer or public page; inline code and fenced code never become math delimiters;
- fenced code blocks render with a header row, readable language label, and compact copy-code button;
- completed code blocks for `ts`, `tsx`, `js`, `json`, `bash`/`shell`, `python`, `go`, `rust`, `sql`, `yaml`, `html`, `css`, `md`, and `diff` highlight through one lazy Shiki singleton that emits `github-dark` and `github-light` token values as CSS variables inside the local semantic code container;
- unknown or absent fence languages render as plaintext with no raw info-string label;
- streaming assistant messages keep code blocks plaintext and do not start Shiki work until the message completes; highlight results are cached by language and code text;
- public share pages use the same `MarkdownMessage`, so highlighting and copy-code behavior have no auth-only assumptions;
- answer heading levels are offset by one (`#` starts at `<h2>`) so embedded Markdown never competes with the shell/public-page `<h1>`;
- paragraphs, headings, inline code, links, citation text, and artifact previews contain unbroken strings; tables, display math, and fenced code own named, focus-visible local horizontal-scroll regions that respond to native keyboard scrolling and never widen the shell;
- blockquotes separated by a blank unquoted line render as distinct blockquotes;
- unsafe links and raw HTML remain inert.

## State Management

Keep server data and UI state separate.

Server data:

- current user/session;
- user-specific provider/model/search catalog;
- saved current-user defaults for provider/model/search/prompt, visibility toggles, and per-model run-control drafts;
- lightweight chat summaries and nested folders/projects with project memory;
- lazily loaded active chat messages and branch tree, with foreground stream completions patched from `chat_update` instead of a post-answer chat refetch;
- prompt presets;
- current-user entitled MCP catalog with preference, configuration/OAuth/runtime readiness, and bounded tool inventory;
- model run request/response artifacts;
- usage and active-chat token statistics.

UI state:

- authentication form drafts;
- open menu/popover;
- command palette query and selected index;
- active Details tab and persisted manual Details mode;
- mobile workspace drawer open/closed state;
- keyed saved-chat, blank-root, and blank-folder composer sessions containing draft, edit intent, staged attachments, pending send/upload/edit ownership, and local operation feedback;
- local streaming buffer;
- Zustand run-lifecycle state for keyed active stream records containing each source chat's run id, optimistic assistant id, and resume ownership, plus cancelled run ids;
- Zustand run-surface state keyed by chat id for the latest compacted Events timeline and decoded persisted run;
- Zustand workspace state for catalog, chats, folders, active chat id, pending folder context, active-chat detail loading/error, workspace loading/error, and the explicit initial-hydration readiness bit;
- Zustand thread state for the active thread messages, active leaf, derived render leaf, visible branch path, and branch fork detection;
- Zustand composer-session state for keyed draft/edit/attachment and async-operation ownership, plus separate composer-control state for selected provider/model/search/prompt, prompt text, visibility toggles, and per-model control draft values;
- Zustand MCP-settings resource state for its coalesced catalog refresh, OAuth callback outcome, and server replacement after mutations; personal input drafts remain local to their server fields;
- independent local general-shell and Settings-scoped notice channels, including background-run `Check run` recovery in the general channel;
- folder collapse state;
- selected local theme id.

Startup loads the backend-filtered catalog before activating the remembered workspace chat so chat defaults win over user startup defaults. Catalog and workspace loaders retain one in-flight promise each; chat detail requests deduplicate per chat, populate a keyed cache, and merge stale responses over concurrent optimistic/token state without replacing newer summary metadata. Returning to a complete cached chat does not refetch detail. Catalog/workspace recovery remains independently retryable and reapplies active-chat defaults only when the active chat and complete next-run control fingerprint remain unchanged, so stale closures cannot overwrite an intervening user edit. The appearance controller reads the local theme preference and mirrors changes to `aiqsa.theme`; `app/layout.tsx` validates that cookie and server-renders both palette and scheme attributes to avoid a mismatched dark class or visible palette flash without making theme a server-side user setting. Dual-theme Shiki output follows later runtime switches without a second highlight pass.

Use React state for local UI. Add a query/cache library only when data invalidation becomes non-trivial.

### PowerAppShell Ownership Boundary

`PowerAppShell` is a composition/controller boundary. It wires stores, hooks, refs, effects, and view modules together, but is not the owner of server data, branch state, or next-run control state. Its root view boundary has exactly seven keys—`session`, `workspace`, `thread`, `composer`, `details`, `settings`, and `overlays`—instead of inheriting the three-digit union of leaf props. Compile-time leaf coverage and component tests make that key set executable.

State ownership:

- Run lifecycle: `runLifecycleStore` owns keyed stream producers/controllers, run ids, cancelled run ids, optimistic assistant message ids, and resume ownership. The shell selects `activeStreams[activeChatId]` directly; late background metadata never becomes the selected chat merely because no other stream is active. Future slices should integrate with this store instead of duplicating producer ownership.
- Run inspection: `runSurfaceStore` owns one in-memory surface per saved chat: the latest compacted Events timeline plus decoded persisted run. Every writer receives the non-null source chat id before asynchronous work; navigation is read-only selection, a new send/regeneration resets only its source, and chat deletion or authoritative non-stream removal evicts only that key. Blank/missing chats share one stable empty snapshot.
- Workspace data: `workspaceStore` owns catalog, `WorkspaceChatSummary[]` without messages/usage, folders, active chat id, pending folder context, active-chat detail loading/error, workspace loading/error/readiness, blank workspace transitions, and folder/list patching. Exact runtime decoders reject malformed list/create/update/branch summaries before state mutation; workspace responses cannot hydrate `threadStore`, even if a future server adds unknown keys. Thread-only updates never replace the workspace array or any summary identity.
- Thread/branch data: `threadStore` owns `ThreadSnapshot` entries keyed by chat id: messages, active leaf, and usage. It owns visible-branch derivation, checkout/delete/edit/regenerate repair, inactive foreground streams, terminal merges, and cache eviction after chat deletion. A blank workspace changes selection without clearing cached/background threads; `PowerAppShell` selects the active key for view wiring.
- Composer sessions: `composerSessionStore` owns keyed saved-chat, blank-root, and blank-folder snapshots containing draft text, editing message id, staged attachments, send/upload generations, one optional pending-edit owner, and operation feedback. Async writers capture a source key/token before awaiting; send snapshots clear visible input immediately and settle by globally unique operation token even after blank-to-chat transfer, while edit ownership is strictly per session. Chat/folder deletion discards late results by removing their source, authoritative refresh evicts remote-missing non-stream chat and blank-folder sessions, and `PowerAppShell` renders only the selected session.
- Composer controls: `composerControlStore` separately owns selected provider/model/search/prompt for the next run, prompt text, visibility toggles, and current per-model run-control draft values. A provider changes only as the provider half of a selected concrete model; model/profile selection writes its complete control tuple atomically, while profile identity remains derived rather than stored. `runControlsActions`, message/thread/workspace/run-lifecycle actions, and shell UI handlers use the focused owners instead of raw root setter bags; `PowerAppShellView` receives grouped composer-pane actions.
- Prompt/settings workflows: `promptSettingsStore` owns Settings prompt editor draft, dialog open state, prompt saving state, delete-confirmation target, and dirty-editor detection. `promptSettingsActions` owns prompt list mutations, default prompt movement/protection, prompt create/update/duplicate/delete, writes only the Settings-scoped notice channel, and coordinates `Use for next run` with the composer/control owner. `PowerAppShellView` receives prompt/settings state and workflows as grouped props instead of raw prompt setters.
- Local UI only: keep modal/drawer/popover/menu openness, confirmation targets/resolvers, command palette openness/query, model picker openness/query, manual Details mode/tab, folder collapse UI, hover/focus-only state, theme selection, the independent general-shell and Settings notice slots, and one-shot leaf presentation state local unless a later task proves a cross-workflow owner is needed. Coherent cross-component sessions belong to the focused appearance/workspace/overlay controllers; ephemeral leaf state remains in its leaf. No raw `set*` prop crosses the root `PowerAppShellView` or `ShellLeftPane` boundary: local mutation intents are named inside their owning feature contract, while `LeftChatPane` remains the final callback-shaped rendering surface.

`LeftChatPane` still uses `sameLeftPaneData` because it remains a memoized prop adapter that intentionally ignores callback identity churn from `ShellLeftPane`. Remove that comparator only if a future slice moves the left pane to direct selector subscriptions or otherwise stabilizes all callback props without reintroducing broad prop comparisons.

## Testability Rules

- Prefer clear visible labels and stable `data-testid` anchors for critical behavior.
- Every state must remain understandable with nonessential motion disabled and deterministic fake data/providers.
- Keep layout ownership stable enough for targeted visual comparison. Use focused browser behavior tests and inspect the affected desktop/mobile states directly for material visual changes.
- Select proportional checks through `TESTING.md`; do not create exhaustive screenshot inventories for routine slices.
