# RUN CONTRACTS

Owner: Run pipeline maintainers
Scope: Provider-neutral admission, context, tools, Search/Knowledge, streaming, recovery, output, usage, sharing, and retention.

## Meaning And Admission

A run starts from one user message on a message-DAG branch, may use Memory, Knowledge, Search, and MCP, and produces a visible answer plus safe citations or generated outputs. Search is optional; internal execution evidence is not the product. Provider/model, Assistant, Search/Knowledge plans, tools, attachments, and controls come from the current server-filtered catalog and are revalidated before acceptance.

Ordinary runs receive the server-rendered standard-chat baseline and visible-answer developer rule. Assistant runs resolve one currently authorized immutable revision and reject client-expanded controls. Assistants grant no dependency entitlement and unavailable governed dependencies fail closed. Accepted requests freeze exact non-secret configuration and dependency bindings, including the installation's positive-integer maximum tool-call, tool-round, MCP Auto discovery-result, and MCP Auto discovery-timeout budgets; later edits affect future messages only. Installation defaults are 20 calls, 8 rounds, 10 tools per discovery, and 60 seconds across one discovery's routing attempts. Administrators may choose larger safe call/round integers without an arbitrary product cap, while one discovery cannot exceed the general 128-tool MCP run-plan boundary or the 120-second interactive discovery boundary; execution or recovery keeps the values accepted with that run.

Project runs receive the same standard-chat baseline plus the Project's current instructions. Project Memory is dormant in Personal Memory v1: admission creates no Project or Personal Memory bindings/items and injects no Memory context. A current Contributor, Manager, or Owner may invoke only the model, Assistant revision, Search, Knowledge, Skill, and shared/no-auth MCP resources in the canonical active Project set; Viewer cannot mutate chats or start runs. Project authority deliberately replaces per-initiator personal entitlement intersection and never expands a personal catalog. Models and Search resolve only installation/shared credentials, Knowledge uses its Project-safe shared embedding authority, and MCP uses shared/no-auth values even when a member has personal overrides. Personal credentials, Memory/history tools, unpublished Skills, OAuth identity, and personal MCP values are excluded, with no fallback or substitution. Admission binds the exact Project access, instructions, policy, resource revisions, and shared credential evidence; execution rechecks accepted access and covered authority before provider I/O, during streaming, before external tools, and before terminal settlement, so revocation or configuration drift stops the run.

The server, never the browser, builds ordered context from the active ancestor path. Siblings are excluded. Empty terminal-error pairs are omitted from later provider replay so retry does not duplicate an unanswered question; accepted partial-answer questions remain. Attachment-only turns use neutral private-data-free markers in replay. Eligible Personal Memory, prompts, attachments, tool transcripts, and current content all count toward one model-specific budget; prior turns are dropped whole, while an irreducible current request fails before run creation.

The first text send in a blank chat derives its bounded title locally inside the run-creation transaction, compare-and-set against only placeholder titles. A blank Project workspace remains client-local until that first send atomically creates the shared chat, user message, and admitted run; failure leaves no empty shared chat. Title creation never calls a provider or creates usage, attachment-only input keeps the placeholder, and shares copy the already-persisted sanitized title.

Admission has two commits only when an eligible Normal personal run may perform Memory utility I/O. Phase A accepts the exact message graph, ordinary bindings, private `preparing` run, bounded base request, and one content-bounded retrieval attempt. Phase B revalidates mutable authority and selected evidence, creates immutable Memory bindings/items, freezes the minimum recovery checkpoint, and makes the run dispatchable. Independent Memory ranking-signal loss may freeze a non-empty `DEGRADED` attempt with its reason, but no degradation bypasses selected-item authority rejoin; its client-safe presentation says Memory was used with limitations, while a fail-safe attempt with no admitted Memory says Memory was unavailable. An empty or rejected set remains non-visible Memory. Temporary and Project runs bypass Personal Memory storage and utility work entirely. No answer provider or tool I/O occurs while `preparing`.

## Search, Knowledge, And MCP

Search is an explicit ordered logical plan. Off, organization inheritance, personal preference, and concrete selected sources remain distinct. Admission assigns one complete deterministic set of exact hosted/query-only routes; it never drops a source or substitutes another connection/model/key. Hosted and client Search remain different disclosure/retention boundaries.

Client Search receives one normalized bounded generated query and server-owned controls, never transcript, prompts, attachments, or original file data. Fan-out merges safe normalized findings deterministically, preserves attributable partial failures, and calls no fallback. Search provider text is only a bounded tool result; Sources/citations are revalidated safe projections.

Attachments do not disable or narrow a selected Search plan. The query-only request shape, rather than a disclosure-consent gate, prevents attachment identity, names, bytes, and extracted text from reaching client Search.

Runtime admission, not browser state or declared model metadata, freezes whether the selected deployment has current credential-scoped Direct PDF evidence. On that route a PDF may be processing or have a local parser failure: the original verified object is sent without extracted text. Without that evidence, the same PDF requires ready non-empty local extraction and no original bytes are read for the answer provider. Missing records and storage, size, checksum, count, or payload-limit failures reject the whole attachment set before provider I/O. Route selection is final for the run; a failed direct provider request never causes an automatic local-parser retry or second answer request.

Knowledge ingestion independently follows the immutable Profile revision bound to its artifact. Local keeps PDF bytes inside the parser boundary; System Model Direct sends bounded original page ranges; System Model Vision sends bounded rendered page images. Model routes use the exact pinned installation role, no Search or tools, a fixed page-marker transcription contract, and provider-reported usage accounting. They do not require Structured Output or silently change route. The current model-PDF profile preserves the complete textual transcription and additionally projects information-bearing charts, plots, diagrams, maps, and figures into bounded searchable text: visible axes, legends, labels, units, series, readable points, and visually evident ranges or trends are retained, while hidden values, causes, and intent may not be inferred. Pure decoration creates no projection. A versioned Vision parser profile may combine the model-authored structure with clean visible native PDF rows from the same immutable original: exact matches contribute coordinates, materially novel non-conflicting rows may fill uncovered regions, and a clean row may replace exactly one aligned non-table Vision paragraph when their normalized token sequences differ only in numeric tokens. The native row must be on the same safe visible page; ambiguous, structurally different, invisible, invalid, or clip-only text remains ineligible, and the complete native change is rejected rather than truncated when a bound would be exceeded. Vision remains the structure owner while clean uniquely aligned embedded PDF text is the value authority. This local native-text lane is neither OCR nor a second provider route. A crash-ambiguous dispatched batch is terminal within its processing generation and is never blindly replayed by recovery. Explicit operator Reprocess advances the failed artifact to a new generation, preserves the old ambiguous attempt, reuses checksum-verified settled batches from older generations, and may dispatch one new bounded attempt for each still-unsettled batch. Geometry-proven repeated page furniture is deduplicated for retrieval, but one canonical source occurrence remains chunkable so a report header cannot erase document-level facts merely by appearing on every page.

With no selected Knowledge scope, the run performs zero Knowledge operations. Otherwise admission binds exact Bases or directly selected Sources, immutable ready Source Versions/artifacts, the active embedding destinations, and authority. `All my knowledge` resolves through the same owner-scoped rules; Project use requires explicit Project bindings, and Assistant selection grants no additional entitlement. Later membership, replacement, Profile, or Trash changes cannot alter accepted evidence. A usable partial parse remains ready evidence with safe warnings. Admission skips a processing-only Profile when another selected Profile is ready; zero ready selected Sources yields `sources_processing` before embedding, retrieval, or answer-provider I/O, while a ready subset proceeds with immutable exclusions and yields `partial_sources_ready`.

Knowledge answer admission freezes the installation answer policy and one route for the run. When the canonical ready corpus is estimated at no more than 70% of the selected model context and the exact final provider request still fits, every admitted passage is supplied as one full-corpus Evidence Manifest: there is no Knowledge tool, query embedding, or retrieval operation, and answer-model tool calling is not required. Otherwise the run exposes exactly one strict tool, `search_knowledge({ query, sourceAliases })`; its schema has no scope IDs, limits, modes, or hidden controls, `query` is normalized non-empty Unicode of at most 3,000 code points, and `sourceAliases` is a unique list of run-local `S…` aliases. The first call uses an empty list. A later call may narrow only to aliases that earlier completed results in the same run already disclosed; aliases are re-resolved against immutable admitted Sources and never grant authority. A whole-Base scope larger than the bounded eager Source-binding set remains authorized in full through its immutable admitted Base snapshot; only Sources selected into bounded evidence receive stable run-local aliases before provider packaging, and this disclosure-bound persistence is neither corpus sampling nor a retrieval scope reduction. The answer model may call the tool within the frozen installation maximum, 12 by default and admin-configurable from 1 through 32 for future runs. Each accepted call is preflighted before external I/O, rechecks current scope authority and Memory egress policy, and has a 60-second execution envelope across query embedding, hybrid retrieval, hosted reranking, and durable receipt settlement. A local Knowledge deadline is persisted as a technical error result so the provider may continue from earlier evidence; user cancellation remains fail-closed and never becomes such a result. The operation, embedding, candidate, retrieved-text, cost, and latency ceilings scale together from that immutable snapshot; the next call beyond it is rejected before embedding or retrieval.

Both routes settle through the same Source-bound Evidence Manifest and citation authority. Answer synthesis uses the bounded adaptive Coverage Planner V20 / Draft V20 / Selector V16 protocol on one immutable answer-model snapshot and byte-identical evidence. Every new Knowledge answer first runs one private Coverage Planner over the exact request and manifest; it must produce a bounded non-empty ordered plan of independently required answer dimensions and cannot answer the request, adjudicate support, or use information outside supplied evidence. That accepted plan is immutable task decomposition, is persisted as private provider-attempt state, and is supplied unchanged to the recall-oriented Draft and precision-oriented Selector. Draft must propose at least one evidence-derived standalone candidate and cannot decide sufficiency; it returns only ordered semantic claim text plus canonical evidence hints, while the server deterministically assigns prompt-local claim IDs and rendering layout. Each candidate contains one independently checkable factual or relational assertion: subordinate, relative, comparative, conditional, and causal links requiring different support remain separate claims. For difference or contrast requests Draft preserves each compared subject's defining positive evidence-backed property and never infers a negative property from silence; for polar requests it proposes the narrowest evidence-entailed candidate that directly affirms or negates the requested relationship. A derived comparison, calculation, explanation, or association need not occur verbatim in a Source, but all operands, labels, qualifiers, units, and the complete relation must be entailed by the cited evidence. Selector adjudicates every candidate and is the sole precision, coverage, contradiction, and semantic-insufficiency authority. It cannot create, rewrite, combine, or repair claims or plan dimensions; it returns claim verdicts, selected server-authored literal IDs, one covered-or-missing status for every exact planned dimension, support IDs, and an insufficiency reason only when nothing is selected. Every covered dimension points only to supported claim IDs or selected literal IDs. The server restores the persisted dimension descriptions and deterministically derives decision, requestCoverage, and ordered missingInformation; it never creates claims, chooses evidence, compares operands, or infers an answer relation. Complete requires every planned dimension covered, partial requires both covered and missing dimensions, and none requires every dimension missing. An explicit all/every/complete-list request remains exhaustive; a broad overview, role, influence, significance, why, or how request uses the smallest complete set of supported mechanisms, constraints, trade-offs, and outcomes, while unrequested background does not create partial coverage. Materially distinct co-equal mechanisms, properties, guarantees, constraints, or outcomes stated by the same answer-bearing theorem, definition, construction, or result remain separate dimensions; intermediate proof steps, repeated examples, ancillary parameters, and topical background do not. For why/how requests a premise restatement is not complete: Draft must propose an evidence-backed explanatory relation and Selector may support it only when the complete relation is stated or logically entailed. Literal spans may recover missing requested direct facts but cannot create a comparison, calculation, association, explanation, polar relationship, or other derived conclusion. When the initial Selector derives complete or none, settlement is the normal three-call path. A deterministic Selector contract violation permits exactly one fresh Selector validation repair over the unchanged plan, Draft, and evidence; provider transport errors, refusals, timeouts, and provider failures do not. Only an accepted partial result over a non-maximal valid Draft may trigger one targeted supplement Draft followed by one final Selector. The supplement uses the same plan and manifest, is independently capped, and is deterministically merged without server semantic inference. Retrieval and reranking are never repeated. Malformed or failed Planner output stops before Draft; malformed supplement stops before a final call; failed verification never publishes unverified Draft text. V16 grounding receipts record the content-free Planner identity plus every Draft and Selector operation, distinguishing validation repair from coverage correction without retaining candidate, verdict, or plan text. Accepted V19/V15, V18/V14, V17/V13, V16/V12, V15/V11, V14/V10, V13/V9, V12/V8, V11/V7, V10/V7, V9/V6, V8/V6, and V7/V5 operations retain their exact prompts and recovery paths; new runs mint only V20/V16.

The separately versioned Snapshot V39 current path inherits Snapshot V38's Draft V21, blind Coverage Scope V6, query-granularity, epistemic-fidelity, answer-level-compression, server-issued request-anchor IDs, and model-owned set reduction preserving append-only Scope Completeness V1, Selector V21, and independent Scope Closure V2. The normal sequence is `Draft -> Scope -> Scope Completeness -> Selector -> Scope Closure`. Scope is a physically separate request/evidence-only model operation: its prompt contains neither Draft, supported-answer content, Selector state, nor a previous coverage decision. The server deterministically exposes bounded evidence units, one per K handle, containing exact sentence/control-run atoms with prompt-local IDs and immutable provenance. Scope reviews every atom and returns exactly one record per supplied handle; each record contains zero or more materially distinct local findings that are already final answer-scope dimensions, not an intermediate positive-atom classification. A cross-unit conclusion is one joint finding spanning at least two K handles, while an explicitly requested unsupported facet has no atom IDs. Negative atoms are never echoed, and there is no separate positive-map-to-scope reduction that could discard a valid finding. Scope and completeness receive a bounded server-authored `Q...` ledger of exact request fragments and select one identifier per finding instead of authoring control-plane anchor text. The server resolves only known IDs to their immutable request substrings before the unchanged V6 validator; an unknown ID or unrelated literal still fails closed, and resolution performs no semantic matching, filtering, promotion, or remapping. The server validates the exact unit-key set, uniqueness, atom ownership, request anchors, joint provenance, and bounds, canonicalizes atom and handle order, and losslessly materializes every accepted finding into immutable Scope; it may order findings and assign D IDs but never filters, summarizes, merges, or semantically classifies them. Snapshot V34 inherits V33 and extends V26's deterministic fail-closed exception for invalid output: when validation proves that a local finding cites an atom owned by another evidence unit, or that a purported joint finding spans fewer than two or more than the admitted number of evidence handles, the server removes that entire invalid finding and revalidates the whole remaining Scope. It never converts a joint finding to local, filters or remaps its atom IDs, transfers provenance, or promotes coverage. The same rule applies if the violation becomes visible only after another verifier-directed patch; every other validation class stays on the bounded verified-repair or fail-closed path. Snapshot V35 adds one global reduce rule without another operation: Selector may exclude an evidence-free unsupported item only when a distinct evidence-backed item with the exact same immutable request anchor survives as covered or missing. The model owns that equivalence decision; the server rechecks only exact anchor identity, positive provenance, and the surviving decision, and never transfers provenance or promotes coverage. Snapshot V35 also makes the primary Draft map output explicitly atomic across co-equal facets: independently verifiable properties, comparison axes, mechanisms, risks, benefits, outcomes, actors, and beneficiaries are separate bounded claims, while a material qualifier stays with the facet it governs. The Selector may combine several independently supported claims to close one compound task; no language-specific server splitting, semantic promotion, schema change, or additional model operation is introduced. Snapshot V36 replaces the lossy covered-only closure view with one holistic reduction-safety audit over the complete ordered pre-reduction Scope and its exact assigned atoms while still exposing answer text only for already supported mapped claims. It can only reopen `covered` or `excluded` to `missing`; it cannot select support, promote coverage, add evidence, or introduce another model operation. Snapshot V37 preserves that audit and admits it whenever Selector made any non-missing decision, including an all-`excluded` reduction; only an all-`missing` result skips Closure because every dimension is already retained for correction. Snapshot V38 changes grouped Supplement interpretation and final rendering, and its existing Scope-repair path composes the inherited whole-finding invalid-provenance rejection only after an isolated replacement remains invalid. Snapshot V39 changes only the Selector's semantic-duplicate representative choice: it ranks each equivalent item's own independently supported answer surface for exact entailment, completeness, and publishable clarity, uses Scope order only as a tie-breaker, and transfers no text, support, atoms, or provenance. Scope, Closure, Selector authority, retrieval, and operation scheduling remain unchanged.

Scope Completeness is a second physically separate model operation over only the exact request, the complete immutable atom ledger, and accepted Scope; Draft, Selector, prior coverage, and retrieval are physically absent. Snapshot V29 makes the request's semantic operator a hard completeness boundary: for why/how/explanation tasks, restating a premise or conclusion is not an answer task; Scope must retain an evidence-backed cause, mechanism, enabling condition, trade-off, constraint, consequence, or other entailed connector, or mark the requested facet unsupported. Snapshot V30 additionally treats eight dimensions as a ceiling rather than a target. Explicit all/every/list, separately named, and multi-part requests remain exhaustive; a broad non-exhaustive overview, role, influence, significance, or general why/how request uses the smallest non-overlapping high-importance task set that answers it. Repeated examples, parameter subcases, proof branches, and background remain evidence for a broader task unless independently requested or necessary as a distinct mechanism, constraint, trade-off, or outcome. Snapshot V31 further prefers the lowest source-stated answer-level proposition that faithfully answers a broad request. A material uncertainty, condition, contradiction, or co-equal key point remains required, while an unrequested example, record row, parameter value, formula term, proof step, or exception inventory does not become another coverage quota. Scope, completeness, Supplement, and both Selector passes apply that same model-owned reduction rule; the server performs no semantic compression or promotion. This adds no schema, retrieval, or model operation. Snapshot V32 introduced cross-unit reduction in both Scope and the global Selector. Snapshot V33 restores the SOTA map/reduce boundary: Scope is recall-first and may retain equivalent locally entailed findings from repeated, paraphrased, tabulated, or independently sourced evidence, while the existing global Selector is the sole semantic redundancy-reduction stage and keeps the earliest equivalent positive item eligible. Scope must never suppress every representative because a fact repeats. Different subjects, relations, polarity, comparison sides, actors or beneficiaries, conditions, uncertainty, attribution, mechanisms, constraints, trade-offs, outcomes, and separately requested members remain distinct; an evidence-free requested facet remains missing unless Snapshot V35's global Selector identifies it as the false no-data placeholder for a distinct evidence-backed item with the exact same request anchor; that peer must remain covered or missing. The server performs no semantic merge, similarity decision, representative selection, or provenance transfer, zero-dimension validation remains fail-closed, and this boundary adds no schema, retrieval, retry, or model operation. Completeness re-reviews every atom, including neighboring cross-unit operands, and may return only materially distinct omitted additions. The server treats accepted Scope as append-only, validates anchors, atom ownership, cross-handle provenance, uniqueness, and remaining capacity, then derives one canonical merged Scope; the operation cannot delete, rewrite, merge, narrow, or replace any accepted item.

Selector receives a de-duplicated exact-text index for all atoms assigned to that merged Scope and independently classifies each positive item as eligible or excluded before coverage mapping. It excludes a positive item only when its complete description is not entailed by its own assigned atoms or it is not a material direct requirement of the exact request. Epistemic force and attribution are truth-conditional slots throughout Scope, completeness, Supplement, and both Selector passes: a belief, expectation, conjecture, estimate, possibility, unknown, limitation, or explicitly omitted proof cannot support a stronger categorical proposition, and an attribution whose actor changes the proposition cannot be dropped. An explicitly requested unsupported facet has no atom IDs and remains missing unless Snapshot V35's global reduction identifies it as a false local no-data placeholder for a distinct evidence-backed item with the exact same request anchor; that positive peer must survive as covered or missing. Among eligible items, Selector maps each Scope ID to covered or missing using only supported claim or selected literal IDs whose canonical support handles overlap that Scope item; related content from the same evidence item does not cover a different conclusion, and evidence presence alone is never coverage. A literal may cover only a complete directly requested fact at the same epistemic force; a fragment, list continuation, or span requiring synthesis cannot establish an explanation, overview, influence, significance, or other derived relation. Excluded items have no support IDs and are absent from correction, settlement publication, missingInformation, and the coverage denominator. The server validates exact IDs, Scope order, request anchors, mapping overlap, hashes, provenance, eligibility shape, and bounds without making the semantic eligibility decision. Before validation, it deterministically removes only duplicate, unknown, provenance-disjoint, or status-forbidden support edges proven invalid by the immutable ID/provenance graph. A covered dimension with no valid edge left is downgraded to missing; the server never promotes coverage, remaps an ID, or chooses support. The normalized whole payload must still pass the unchanged Selector validator, while genuine shape, D-ID, order, and status failures retain the bounded repair path.

Scope Closure V2 then receives the exact request, the complete ordered immutable merged Scope, every Scope item's exact assigned atom sequence, Selector status/support IDs, and the supported answer projection restricted to claims already mapped to covered dimensions. It independently decides whether mapped text collectively entails every material semantic slot of each covered dimension and whether each exclusion is valid under the exact request and assigned evidence. Explicit set cardinality and quantifiers are truth-conditional: one member or a narrower proposition cannot subsume a requested collective, complete named set, or otherwise distinct earlier requirement. Closure runs whenever at least one Selector decision is `covered` or `excluded`; an all-`missing` result alone proceeds directly to correction. Closure may only preserve state or reopen `covered`/`excluded` to `missing`; it cannot promote a missing dimension, create an exclusion, select support, alter evidence eligibility, rewrite claims, or inspect unrelated answer text. Selector, Scope Closure, Supplement, and final Selector pin the same merged Scope hash. Every supplemental claim is bound to exactly one initially missing or closure-reopened Scope ID with positive evidence, and all such IDs receive a candidate. The Supplement prompt's sole factual input is a deterministic bounded index from each target D to all exact immutable atom IDs/text assigned by Scope; the full manifest, unrelated handles, and primary Draft are physically absent. The index is either complete or correction is skipped, never truncated. Supplement returns only target ID and claim text; the server derives its advisory Draft handles from that target's immutable atoms. Each independently falsifiable subordinate, relative, comparative, conditional, causal, enabling, purpose, or consequence relation is a standalone claim. Supported component facts do not authorize an unstated connector or explanation. Each target's upper bound derives from the exact target count, the 24-claim complete-Draft limit, and the eight-dimension Scope limit: at most three atomic claims per target, fairly reduced when primary claims leave less capacity. These bounds are ceilings rather than quotas, and the complete merged Draft never exceeds 24 claims. This projection adds neither retrieval nor server semantic inference. Snapshot V35 may remove only NFC-exact repeats of immutable primary Draft claims from a grouped Supplement before validation, and only when every target group remains non-empty; if any target would empty, the original payload fails closed. It performs no fuzzy deduplication, rewriting, target movement, coverage inference, or additional model call. Snapshot V38 validates duplicate text inside each immutable target group instead of across their flattened union: NFC-exact replicas in different targets receive separate server-owned claim IDs and retain only their own target handles, while same-target and primary-Draft duplicates still fail closed. Merge preserves every replica in target/claim order so the final Selector adjudicates them independently. Only after Selector, the renderer removes exact repeated supported text, keeping the first Draft-order replica and only its citations; coverage continues to count every target-local identity.

The final least-authority verifier receives the immutable base protocol state, target descriptions and bindings, supplemental claim text, a bounded projection of already-supported primary map points whose complete accepted handle set is contained by at least one exact correction target, and the same complete target-only atom index; the full manifest, unrelated atom ledger, literal index, and every other primary Draft text remain physically absent. Primary map points are candidate protocol state, not factual evidence. The verifier first rechecks each positive correction target's eligibility, then revalidates every admitted primary point and supplemental statement against that target's exact atoms and request, including actor, recipient or beneficiary, risk/value-flow direction, comparisons, conditions, negations, qualifiers, attribution, and epistemic status. It may cover a target with any collectively complete union of revalidated listed primary points and supported target-bound supplemental points; either source may be absent when the other fully closes the target. A prior verdict, handle containment, lexical overlap, or candidate membership alone never establishes relevance or coverage. It may veto an evidence-bearing false-positive target to `excluded`, but an explicit requested unsupported facet never enters correction and cannot be hidden; an eligible target whose accumulated map remains incomplete stays missing. Only target-bound support whose handles come from that target's immutable provenance may close it. Accepted primary verdicts and support handles, extracts, closure-preserved covered mappings, and non-target eligibility remain immutable. The server uses handle containment only as an authority bound, performs no text matching or semantic mapping, and purpose-bound discards support attached to an excluded target or foreign target provenance. If every exact-target supplemental claim is supported exclusively by that target's provenance but the first final map still leaves the target missing, the server records a content-free `selector_coverage_invalid` failure and may run one fresh target-only final verifier when the eighth slot remains. It never promotes coverage itself, never supplies the rejected payload as evidence, never retries twice, and accepts a second valid partial result fail-closed. Thus the five-call normal path adds no call; only this deterministic final-delta consistency review may add one bounded call.

The V21 canary may assign different reasoning effort to Draft, Selector, the shared Scope/auditor role, Scope Closure, and Supplement only through an explicit installation policy validated against the already accepted answer deployment's capabilities. Its default inherits the accepted answer effort. The policy can never change the answer provider, model, credential, binding, or egress destination; malformed or unsupported overrides fail before answer-provider I/O. The first accepted V21 operation snapshot freezes the append-only `scope_v6_completeness_v1_targeted_delta_v4_repair_budget_v1_claim_surface_v1_target_groups_v1_claim_markup_boundaries_v1_selector_support_edges_v1_collective_target_support_v1_scope_repair_feedback_v1_target_closure_v1_verified_scope_patch_v1_scope_closure_v1_repair_reserved_correction_v2_source_ordered_context_v1_least_authority_delta_v1_fail_closed_local_provenance_v1_final_delta_repair_v1_supplement_atomization_v1_scope_multi_diagnostic_repair_v1_selector_repair_diagnostic_v1_fail_closed_selector_edges_v2_adaptive_atomic_supplement_budget_v1_query_intent_completeness_v1_query_granularity_epistemic_fidelity_v1_answer_level_compression_v1_request_anchor_ids_v1_scope_set_reduction_v1_scope_recall_map_v1_invalid_provenance_rejection_v2_unsupported_supersession_v1_supplement_exact_duplicate_reduction_v1_draft_coequal_facet_atomization_v1_target_accumulative_reduce_v1_global_scope_closure_v1_non_missing_closure_admission_v1_target_local_supplement_v1` pipeline identity and complete effective policy. Scope Completeness pins the initial accepted Scope hash; every Selector, Scope Closure, Supplement, and final request pins the merged Scope hash, and recovery byte-compares each reconstructed request instead of consulting current installation policy. Scope, Scope Completeness, initial Selector, and Scope Closure may each receive exactly one adjacent fresh repair only for deterministic output-validation reasons over unchanged authority inputs; provider transport errors, refusals, timeouts, provider failures, and semantic dissatisfaction are not retried. The final-delta consistency review is a separate deterministic protocol-validation retry: it requires the server-verifiable all-supported/target-still-missing signature and does not reinterpret semantic evidence. The normal path remains five calls. One semantic correction remains exactly the two-operation `Supplement -> final verifier` path. Admission is atomic: the current protocol's eight-call hard cap keeps both correction slots available after any one adjacent structural repair. A second repair may consume that reserve and may therefore leave a verified partial answer instead of starting an over-cap correction; the final-delta review likewise runs only if that same final slot remains. Repairs never create another correction cycle or repeat retrieval or reranking.

The Scope repair receives the broad first validation reason plus a bounded, validator-ordered set of every independently detectable stable-path diagnostic in the transient candidate. Each server-authored diagnostic contains only a code, JSON path, counts, applicable limit, and expected K handle; container or key-set failures whose repair could invalidate descendant paths deliberately fall back to the first diagnostic. The set is corrected in the existing single repair call, so this contract adds no operation. The initial Scope pass retains the V24 request/evidence-only authority and payload-schema lineage; the verified-patch system contract also names the fault-isolated merge that is activated only after rejection. The repair receives the machine-readable global and per-array limits and, when the rejected payload has the exact bounded Scope root container, a hash that pins its private crash-recovery state without exposing its content in the repair prompt. Invalid leaves do not invalidate that transient boundary. For a local finding diagnostic, the server takes only the repair candidate's evidence unit with the matching immutable K handle and replaces the rejected base unit; for a joint-finding or unsupported-dimension diagnostic, it replaces only that bounded collection. For a missing, duplicate, malformed, or incomplete unit map, the server preserves each expected K unit that occurs uniquely in the bounded base, discards any indexed rejected unit, and fills only absent or rejected handles from the repair candidate in immutable evidence order. If either side cannot yield exactly one valid unit for every expected handle, repair fails closed. It revalidates after every replacement and preserves every unrejected map unit and collection. Only a malformed root container without reusable bounded arrays requires one independently valid fresh replacement. The rejected provider payload never becomes evidence and never relaxes Scope V6; recovery persists and byte-compares the exact diagnostic and repair-base hash. V37 retains the historical isolated-replacement failure behavior. V38 first attempts that same target-unit replacement, then composes the existing whole-finding invalid-provenance rejection only when the replacement remains invalid; it removes the complete finding rather than filtering or remapping its atom IDs.

Before the first Draft request, current Evidence Manifest packing enforces the downstream source-ordered atom capacity in addition to its byte and token budgets. Exact retrieved excerpts are never shortened. When the next ranked item would exceed any bound, its optional expanded context is omitted as one explicitly labelled field and the complete candidate is rechecked; if its exact excerpt still cannot fit, that lower-ranked item is excluded under the existing budget reason. Every retained item is therefore visible byte-identically to Draft and every later grounding stage, and an over-cap manifest fails or compacts before answer-provider I/O rather than after a paid Draft.

The initial Selector pass likewise keeps the exact V25 prompt. Only a deterministic coverage-map rejection gives its existing single repair call a server-authored content-free diagnostic containing the stable JSON path, expected D identity or count, and permitted K handles; rejected claim IDs, support IDs, claim text, and provider payload are absent. Recovery persists and byte-compares this diagnostic, and the repair gains no semantic authority or additional operation. Current Selector edge normalization is likewise operation-free and may only delete graph-invalid edges or downgrade an unsupported covered decision to missing; historical V26 validation remains strict.

Before authoritative Draft or Supplement validation, V17 retains V16 canonicalization, support-edge, collective-support, and complete-target-closure semantics. For expanded passage evidence, the server persists content-free UTF-16 boundaries and Source ordinals derived from the authorized immutable Source, then constructs each atom ledger in trusted Source order as previous context, exact retrieved excerpt, and next context. Provider-facing labels remain framing only and can never define order or provenance. Historical evidence without this metadata retains its exact atom contract. Supplement resolves every target's full ordered atom set before emitting the smallest claim group whose union covers every entailed subject, relation, case, qualifier, and requested outcome. Within one evidence unit, explored hypotheses, proof branches, and intermediate cases remain provisional until later qualifications, exclusions, and the final classification are resolved; ordering never arbitrarily resolves conflicts between independent units. Selector applies the same rule to initial and supplemental claims, marks a branch contradicted when the same unit later rules it out, and covers a target only with an individually supported, collectively complete surviving support set. No individual atomic claim must repeat the full compound target. Unrelated, redundant, unsupported, partial-case, or provenance-only claims cannot establish coverage. Supplement uses a request-specific strict map from every exact positive missing D ID to a required non-empty bounded claim list. The server divides the immutable global claim capacity fairly across all targets, canonicalizes groups in Scope order, derives each claim's advisory provenance only from that target, and never lets one target consume another target's required slot. Final Selector remains the sole support and coverage authority.

The raw provider payload is never product persistence; only the benchmark runner may retain a raw Supplement provider result inside its existing ignored private-artifact boundary, while production keeps only the accepted canonical payload or a content-safe classified failure. A Scope failure may persist the content-free hash of a transient bounded Scope-container repair base, including one with invalid leaves, never the base itself; losing that transient state before repair fails closed rather than accepting whole-payload drift. Grounding Evidence V55 retains V54's bounded content-free cross-target exact-repeat count plus separate content-free initial-Scope, completeness-addition, merged-Scope, coverage, and closure hashes/counts, including initially covered/excluded, reopened-covered/reopened-excluded, final covered/missing/excluded, and total reopened dimension counts, plus repair roles, policy and governed-binding fingerprints, final Draft count, and provider-reported per-operation timing/usage; its version distinguishes the immutable V39 Selector contract. Raw findings, raw claim text, private Scope content, closure decisions, repair-base content, repair-path content, invalid-provenance content, and compressed-away subordinate detail are not projected into it. Aggregate metrics project only versions, enumerations, counts, durations, and token usage—never request, evidence, atom, claim, scope description, mapping, literal, answer, diagnostic path, expected handle, or private identity text.

Snapshot V37 and Grounding Evidence V53 retain non-missing Closure admission with the historical flattened grouped-Supplement duplicate interpretation and pre-V38 rendering. Snapshot V36 and Grounding Evidence V52 retain the holistic Closure V2 audit with the historical covered-only scheduler. Snapshot V35 and Grounding Evidence V51 retain the global reducer, exact Supplement duplicate reduction, primary-Draft co-equal facet atomization, and target-local accumulative reduce without the holistic audit. Snapshot V34 and Grounding Evidence V50 retain deterministic whole-item invalid-provenance rejection without those reductions. Snapshot V33 and Grounding Evidence V49 retain the recall-first Scope map and Selector-owned global reduction without whole-item rejection for invalid joint provenance. Snapshot V32 and Grounding Evidence V48 retain cross-unit reduction in Scope as well as the global Selector without the recall-first Scope correction. Snapshot V31 and Grounding Evidence V47 retain answer-level compression and server-issued request-anchor IDs without model-owned cross-unit set reduction. Snapshot V30 and Grounding Evidence V46 retain query granularity and epistemic fidelity without answer-level compression. Snapshot V29 and Grounding Evidence V45 retain query-intent completeness without query-granularity or epistemic-fidelity prompt rules. Snapshot V28 and Grounding Evidence V44 retain target-count-aware atomic Supplement capacity with the historical Scope/completeness prompts. Snapshot V27 and Grounding Evidence V43 retain deterministic fail-closed Selector edge normalization with the historical flat 12-claim Supplement allocation. Snapshot V26 and Grounding Evidence V42 retain repair-only content-safe initial Selector diagnostics without fail-closed unknown-edge normalization. Snapshot V25 and Grounding Evidence V41 retain single-call multi-diagnostic Scope repair without repair-only Selector diagnostics. Snapshot V24 and Grounding Evidence V40 retain atomic Supplement publication with the historical first-diagnostic Scope repair. Snapshot V23 and Grounding Evidence V39 retain the final-delta consistency review without atomic Supplement publication. Snapshot V22 and Grounding Evidence V38 retain fail-closed local provenance rejection without the final-delta consistency review. Snapshot V21 and Grounding Evidence V37 retain target-only least-authority delta verification without deterministic local provenance rejection. Snapshot V20 and Grounding Evidence V36 retain trusted source order with the historical full-context final Selector. Snapshot V19 and Grounding Evidence V35 retain the repair-reserved eight-call correction budget without trusted source-order metadata; Snapshot V18 and Grounding Evidence V34 retain the independent Scope-closure veto with its original seven-call budget; Snapshot V17 and Grounding Evidence V33 retain verifier-directed Scope patch merging without the independent semantic closure veto; Snapshot V16 and Grounding Evidence V32 retain their exact complete-target-closure semantics without verifier-directed repair merging; Snapshot V15 and Grounding Evidence V31 retain structured Scope repair without target closure; Snapshot V14 and Grounding Evidence V30 retain collective target support without structured Scope repair feedback; earlier versions retain their exact historical contracts. Auditor V1/V2, blind Scope V3/V4/V5, Selector V17/V18/V19/V20/V21, Snapshot V6 through V37, and Grounding Evidence V17 through V53 remain exact historical decoders and are never reinterpreted.

Current V39 recovery accepts Snapshot V39 with Scope V6, append-only Scope Completeness V1, Selector V21, query-granularity, epistemic-fidelity, answer-level-compression, server-issued request-anchor-ID, recall-first Scope-map, quality-ranked Selector-owned set reduction, primary-Draft co-equal facet atomization, target-local accumulative reduce over revalidated primary and supplemental map points, and the holistic complete-Scope Closure V2 reduction-safety audit admitted for every non-missing Selector decision, plus target-only atomic generation and least-authority statement verification, bounded positive-target veto, repair-reserved budgeting, deterministic claim-surface canonicalization, exact target groups, delimiter-aware plain-claim validation, deterministic fail-closed removal of duplicate, unknown, provenance-disjoint, and status-forbidden support edges with missing-only downgrade, collective target support-set entailment, content-safe Scope repair feedback, complete target-group closure, trusted source-ordered contextual atoms, ordered same-unit conclusion resolution, fault-isolated Scope map-unit repair over a bounded transient root container, one bounded final-delta consistency review, single-call Scope repair feedback that enumerates every independently detectable stable-path violation, repair-only content-safe initial Selector coverage-map diagnostics, target-local cross-group duplicate interpretation, and post-Selector supported-text reduction. A local failure can replace only the repair candidate unit matching the rejected immutable K handle; joint and unsupported failures can replace only their respective collection. Every unrejected unit survives byte-for-byte and every replacement is revalidated. If an isolated replacement still fails a V34 invalid-provenance check, V39 removes only that complete rejected finding and revalidates the remaining Scope; it never filters or remaps atom IDs, and a zero-dimension or unrelated failure still rejects the operation. Neither validation path promotes coverage server-side or treats a rejected payload as evidence. The quality-ranked reducer, target-local duplicate interpretation, Draft atomization, accumulative target reduce, holistic closure audit, non-missing admission rule, map-unit isolation, provenance fallback, and final render reduction add no operation and preserve fail-closed settlement validation. The eight-call budget still preserves the complete correction pair after one structural repair; V38/V37/V36/V35/V34/V33/V32/V31/V30/V29/V28/V27/V26/V25/V24/V23/V22/V21/V20/V16 recovery remains unchanged.

After V38 qualification, the KB/RAG architecture is frozen. A further architecture change is admissible only when the same deterministic defect appears in at least two independent cases, can be expressed by a neutral synthetic test without benchmark or domain-specific rules, and adds no LLM call or major boundary change. A provider timeout, judge disagreement, one stochastic omission, wording-only difference, ambiguous reference, or isolated case whose correct evidence was delivered without systematic loss is recorded for stabilization but does not reopen architecture.

Provider-native wrappers such as `citeK1K2` are accepted only inside the model-to-server evidence protocol and only when every handle belongs to the final manifest; malformed or unknown handles fail closed. They are never copied from Draft claim text into the public answer. Knowledge token deltas are withheld until grounded settlement completes, so internal status lines, hidden operations, and provider citation wrappers never become visible answer prose. A rendered citation opens the existing authenticated Source rail at the exact accepted version and page; when retained PDF coordinates exist, a bounded private page render highlights them, otherwise the original page and normalized excerpt remain the fallback.

Each automatic call attempts one query embedding across every compatible admitted Profile and performs one hybrid repository operation. Normal passage retrieval combines PostgreSQL exact/metadata/document/section signals, pgvector dense retrieval, and the versioned OpenSearch BM25 lane before deterministic fusion and hosted reranking. OpenSearch applies owner and immutable artifact scope before top-K, returns identities and scores only, and PostgreSQL revalidates every candidate. Missing, stale, unavailable, malformed, or incompletely revalidated OpenSearch evidence fails the Knowledge operation visibly; no PostgreSQL passage-lexical fallback or legacy ranking receipt is selected. A classified query-embedding-only timeout, connection/rate-limit/provider outage, or unavailable embedding configuration disables only the semantic lane, records bounded degraded evidence, and continues the remaining authoritative and BM25 lanes; PostgreSQL, OpenSearch, authority, SQL, invariant, and unrelated failures remain visible failures. Candidate signals must pass their named lane-specific relevance eligibility before deterministic fusion; exact equality remains eligible and a weak nearest vector alone never enters answer context. Canonical Source and content-hash deduplication precede deterministic soft diversity: a close alternative Source may be promoted, but one clearly stronger Source may fill all sixteen broad-map primary slots from the bounded canonical pool. A later automatic search may reduce to Sources disclosed by earlier evidence and then returns at most eight primary slots. Every later search in the same run excludes content hashes already delivered by earlier searches before final ranking and same-Source context expansion, so bounded follow-ups spend their slots on novel evidence while earlier handles remain available in the provider conversation. An unstructured primary may carry at most one previous and one next same-Source block. A complete table-row primary may instead carry a bounded set of complete nearby rows and independently relevance-eligible complete rows or body passages from the exact same immutable Source; these secondary segments never become primary results, never introduce another Source, remain individually atomic, and are omitted whole rather than truncated when the provider evidence budget cannot fit them. When no candidate remains eligible, `no_relevant_evidence` is a completed bounded tool result that explicitly forbids inventing a Knowledge answer; the answer model continues normally.

Each provider-visible Knowledge result contains Source-bound blocks with answer-scoped `[K…]` handles, safe Source labels, exact immutable Source Version/artifact bindings, locators, primary excerpts, and any clearly separated same-Source neighbor context. Source content is untrusted data, never instructions, and the result explicitly states that it is not proof of exhaustive corpus coverage. A cited handle is valid only when a completed persisted `search_knowledge` call has exact stored provider text and the provider-running checkpoint proves that result was delivered before synthesis; deleted, malformed, unknown, failed, pending, or later undispatched handles fail closed. Before answer synthesis, a new tool-loop run packs the best remaining rank from each completed search operation before advancing to the next result rank, then applies the immutable provider-evidence byte/token budget. This bounded rank interleaving prevents an early broad query from consuming the entire final manifest while preserving each retrieval operation's own ranking; the selected policy is frozen in the accepted run and persisted manifest. Historical accepted runs without that selector retain chronological operation-first packing during recovery. Headerless table rows remain association-ambiguous unless the immutable chunk profile proves one complete bounded inline label/value pair from exactly two cells in a singleton table or two structurally separated cells; adjacent multi-row pairs, multi-value rows, projections, and lexical proximity alone do not acquire that authority. Normal claim text contains no citation markers, Markdown, HTML, or newlines. The deterministic renderer owns escaping, citation placement, claim order, and the fixed partial-coverage or insufficiency note; the server never derives a missing semantic conclusion from literal operands.

`read_source`, `find_exact`, and `discover_sources` remain internal deterministic services and historical-recovery artifacts, never answer-model tools. Generic recovery replays settled tool results from their durable checkpoints, reauthorizes an unsettled Knowledge call before dispatch, and never repeats a settled or crash-ambiguous external side effect. Historical focused Knowledge runs retain their original request, manifest, status-line, citation, and recovery decoder; new admission never creates that path. Permanent Source or Base deletion preserves generated answer text but scrubs private evidence and provider payload to safe tombstone state, so later citation resolution exposes no private filename, excerpt, locator, or identity.

MCP extends the same tool loop. Ordinary runs choose Auto, Load all, or Off, and that mode changes only through an explicit user action. Auto freezes a private schema-free capability catalog, begins with only `find_tools(goal)`, and asks the exact admitted System Model for no more than the discovery-result limit accepted with that run. Its first provider round disables parallel tool calls; Load all and later rounds retain the admitted model's ordinary parallel behavior. If a provider still emits multiple valid `find_tools` calls in one round, the server combines them into one bounded routing operation, materializes one selection, and returns that same selection under every original call ID. Verified strict structured output decomposes the combined goal into distinct MCP outcomes and maps tools to each; a first pass that explicitly leaves an outcome uncovered receives at most one corrective routing request within the single accepted discovery deadline. Routing receives bounded branch/current text plus compact server/tool/argument summaries, excludes already loaded tools, has no lexical, regex, embedding, provider-native, or model fallback, and fails closed when an invoked route is unavailable or invalid; an Auto run that never calls `find_tools` has no System Model dependency. Selected IDs are revalidated against the frozen catalog and current materialization authority. The active subset grows monotonically under the ordinary MCP tool/schema/context bounds, with no separate discovery-wide tool cap. Every original selection call, including a coalesced or empty one, is checkpointed by persisted tool-call ID with exact runtime bindings before new schemas reach a provider; execution and recovery replay that checkpoint without rerouting. All successful routing usage, including a corrective request when made, is attributed to the same run separately from answer usage. Router failure exposes only `mcp_auto_discovery_unavailable`. An unrelated unavailable enabled server is not a run dependency. Load all and Assistant allowlists remain exact dependencies and fail closed when unavailable. Administrator policy is validated over the complete sanitized inventory and active revisions affect future admission only.

Internal strict-schema utility work uses the installation System Model through its exact admitted role and immutable credential lineage. A dependent feature may use it only when the current tuple carries verified structured-output evidence; absence, stale evidence, or an unsupported adapter is an explicit unavailable capability and never triggers provider or model fallback.

Memory semantic sorting may instead bind the independently selected installation reranker role. Its immutable snapshot is score-only and does not require Structured Output; ordinary candidate pools use one request unless the complete provider-neutral envelope requires a bounded split. Sorting is atomic across that logical pool: every batch must use the same governed snapshot and return exactly one valid score per candidate. Any missing, duplicate, unknown, invalid, failed, cancelled, or snapshot-mismatched result discards every semantic score and retains the complete fused RRF order; each failed or uncertain batch has at most one fresh retry inside the admitted deadline. The generative System Model sorter remains only the versioned compatibility path when no dedicated reranker is selected. A selected-but-broken reranker never falls through to that path, while the distinct global aggregation role remains strict generative System Model work. Reranker destinations and consent evidence are tracked separately from System Model and embedding destinations.

Skills are immutable text instructions selected manually or linked by ordered definition ID from an immutable Assistant revision; they are not executable tools. Assistant selection does not clear or lock manual Skills. Admission builds the effective list in Assistant order followed by manual selection order, removes repeated definition IDs without reordering, and binds each exact current revision. Ordinary runs resolve every remaining Skill through current user/group/installation authority; Project runs resolve only canonical active Project Skill publications and separately bound Assistant dependencies, never a member's personal Skills. The full effective text is rendered once into an exact hidden user-level context item immediately before the unchanged current user message; it never enters system/developer instructions or visible chat. That item is irreducible in the ordinary context budget, so prior turns and attachment text may yield first but Skill instructions are never truncated; an over-budget request fails before provider I/O. Adapters preserve the same ordering whether they emit adjacent user messages or merge them, while request previews replace the Skill text with a fixed omission marker. The accepted rendered item is persisted in normalized context and recovery reuses it without resolving live Skill state. An Assistant grants no Skill entitlement: every audience, including a Project, must receive compatible direct authority for every linked Skill. Removing a Project resource atomically removes dependent Project Assistants and their defaults rather than leaving a partial authority set. Skills do not auto-activate, execute code, load resources, or grant MCP authority; later edits and publication changes affect future admission only.

Requested tool batches are durably recorded before bounded execution and results return in provider order. Settled calls replay; crash-ambiguous external side effects do not. Every round re-applies the context, output, accepted call, and accepted round budgets. Reaching either tool budget disables further tool choice for final synthesis and produces a user-visible reached-limit warning; a model without tool capability explicitly runs with none rather than mutating persistent tool settings.

When the answer model rewrites an automatic Knowledge query, its single embedding batch and the lexical lanes retain both the exact current user question and the model query. Semantic candidates from those two inputs are deduplicated before ranking, so a rewrite cannot erase the original retrieval intent or gain weight merely by finding the same passage twice. Prior-result exclusion makes later calls advance through novel evidence, while any Source aliases remain the authoritative scope filter.

## Streaming, Recovery, And Settlement

Normalized SSE publishes live lifecycle, answer deltas, a bounded semantic tool-activity projection, safe sources/artifacts, actionable warnings/errors, usage, and terminal completion. Presentation-only synchronization stays transient. Text may stream per token, while durable partial text/checkpoints are batched. The visible assistant message contains the answer and may summarize tool name, server name, round, state, duration, and reached-limit warning; it never exposes request previews, Search traces, provider parameters, tool arguments/results, internal identifiers/errors, raw event timelines, or usage receipts.

Authenticated Project members may read the same client-safe persisted chat projection regardless of which member initiated a turn. That projection includes author attribution, durable partial answer text, normalized active/terminal lifecycle, semantic tool activity, and safe sources/artifacts; it does not broaden the SSE contract or expose private execution evidence.

An authenticated Project event stream carries ordered invalidations, not business mutations or private event payloads. Delivery reauthorizes current membership, projects current client-safe state from authoritative rows, replays retained cursors after reconnect, and requires a canonical resync when the cursor is too old. Access loss closes the stream and future Project reads remain privacy-neutral. Heartbeats preserve transport only and do not imply presence.

Only provider-specific terminal proof permits completion. Truncation, malformed or over-budget streams, timeout, cancellation, safety overflow, and provider failure keep accepted partial text and reported usage where available but never write a contradictory success. Terminal persistence and the status-gated winner occur transactionally; one writer wins completion/cancel/failure.

Recovery uses purpose-bound checkpoints, exact accepted context/bindings, settled tool results, and provider-native handles. It never reconstructs private truth from browser data or provider prose, repeats an ambiguous side effect, or converts a terminal safety/timeout outcome to success. Boot and route-triggered stale reconciliation use guarded age/status boundaries and cannot sweep a newer live run.

Cancellation stops later rounds and durable stream writes promptly. Provider timeout remains distinct from user cancellation. Public and client errors expose stable codes, concise actionable copy, and bounded counters only—never payloads, endpoints, credentials, reasoning, tool arguments, private retrieval text, or storage facts.

## Outputs, Usage, And Retention

Completed UI exposes answer text, safe inline citations and Sources when present, generated outputs, ordinary message actions, Branches, a compact tool-activity summary, and explicit feedback for approved mutations. Live UI exposes factual progress, Stop, recovery, Retry, and actionable failure. Reasoning may be shown only through its reviewed normalized output; internal traces are not inferred or reconstructed.

Usage derives only from provider-reported categories. Answer rounds, client Search, Knowledge embeddings, and Memory utilities retain their own exact provider/model attribution and then contribute to aggregate accounting where applicable. Missing failed-call usage and price are null, never estimated; cached/reasoning tokens are not inferred. Recovery replaces or monotonically enriches the same outcome instead of double counting.

Anonymous sharing snapshots the active visible branch through the positive sanitized schema in [Backend](BACKEND.md). A grounded Gemini live-only branch is not shareable because durable content does not exist. Branch checkout is history navigation, not a run-inspection surface.

Persist only what execution, recovery, side-effect safety, security, deletion/retention, citations/outputs, or aggregate accounting consumes. Logs remain structured and content-free. [Providers](PROVIDERS.md) owns wire terminals, [Memory](MEMORY.md) personal context, and [Persistence](PERSISTENCE.md) durable mechanics.
