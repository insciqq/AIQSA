# UI v2 visual baselines

Playwright owns the deterministic dark/light gallery screenshots in this
directory. Update them only after inspecting the rendered state against the
approved UI revamp prototypes and the current bounded frontend contracts.
