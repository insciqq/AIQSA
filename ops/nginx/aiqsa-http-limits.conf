# Install this file in an Nginx `http` context, for example
# `/etc/nginx/conf.d/aiqsa-http-limits.conf`, before enabling the AIQSA site.
# The site consumes this exact zone name for the upload endpoint.
limit_conn_zone $binary_remote_addr zone=aiqsa_upload_per_client:10m;
