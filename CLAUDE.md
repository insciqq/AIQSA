# CLAUDE

Use `AGENTS.md` as the entry point for this repository.

AIQSA is an agent-built QSA web UI: Question -> Search -> Answer with transparent provider/API/search control. The current UI is a conversation-first workspace with a dark default and optional Classic palettes. Follow the current harness docs and the codebase state.
